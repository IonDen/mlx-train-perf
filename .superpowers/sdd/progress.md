# mlx-train-perf — subagent-driven execution ledger

# ═══ 0.3.0 (started 2026-07-14) ═══

- C0 COMPLETE (controller, 2026-07-14, TDD RED→GREEN): the per-command-buffer launch guard with exact-causal costs landed in `attention/kernel/launch.py`. NEW: `causal_pairs`/`range_macs` (exact triangle MAC accounting, `pair_cost = macs_per_row(n=1)`), `plan_budgeted_ranges` (greedy ascending ranges; models mlx buffer composition — live inputs counted once per modeled buffer + output credits ONLY where chunks provably stay live (fwd O/L, dQ chunks); dkv credits NOTHING (chained intermediates allocator-recyclable — crediting could model commits reality skips, the only unsafe direction); commit at `_PACK_COMMIT_ELEMS = 51<<20` / `_PACK_COMMIT_OPS = 51` = the LARGEST arch class so a commit is never predicted early); per-launcher wrappers `plan_fwd_dispatches`/`plan_dq_dispatches`/`plan_dkv_dispatches(hkv, causal)`; `check_fwd_budget` re-signatured; `MAX_TOTAL_SECONDS` + `_check_launch_budget`/`_rows_within_dispatch_budget`/`_rows_per_dispatch` RETIRED; `_calibrate_fwd(causal=)` credits causal-true MACs (probe SIZING stays nominal = safe direction); launchers gained TEST-ONLY `_force_ranges` (the `_flip_causal` precedent) because the planner correctly never splits tiny packed-regime shapes — chained/split contract tests force ranges explicitly now. Two regimes: own-buffer (flagship: unique inputs ≥ threshold → every dispatch its own buffer → chain totals UNBOUNDED — the 0025 headline) and packed (small footprints → whole chain ≤ one 0.5 s buffer budget — TIGHTER than the retired 2.0 s cap, and production small-n chains are single-dispatch anyway). MAX_DISPATCH_SECONDS 0.5 now explicitly the per-BUFFER worst-day pin (unchanged value). Gates: default 739 passed, FULL METAL LANE 1375 passed/4 smoke-skips, ruff, mypy clean. Projected new dkv cap at 0.2.0 session rates: NONE launch-side (memory decides — the sweep will find it). NEXT: riders (0016 qwen2, 0023 flake, 0022c/d/e/f), then the time-boxed C1 interleave rung, then freeze+campaign.
- 0.3.0 KICKOFF + T1 EVIDENCE STUDY DONE (2026-07-14, controller; Denis's standing directive: "0.3.0 developed and tested, ready to be pushed. Make key decisions yourself" — scope ruled by the controller under that delegation): branch `dev/v0.3.0` from `ae45024`. Backlog: 0018 row moved to Done; 0024 filed (release tracker, flagship = move the context ceiling); 0025 filed (disproof, below); 0016/0023 promoted ready; 0008 deferred AGAIN with rationale (its own step-5 data: perfect fused CE backward barely beats staged). Spec `docs/superpowers/specs/2026-07-14-mlx-train-perf-0.3.0-design.md` + plan `docs/superpowers/plans/2026-07-14-mlx-train-perf-0.3.0-plan.md` (9 tasks). **T1 VERDICT — the 0.2.0 chain-total kill model is DISPROVEN (backlog 0025, 3-part review parts 1-3 same-day):** mlx 0.32.0 `device.cpp` L512-515 commits a command buffer at >50 ops OR >50 M unique input+output ELEMENTS (g13s; data_size() is elements; outputs count; resets per commit) → at flagship shapes EVERY dQ/dK/dV dispatch is its own buffer; the interactivity kill unit is the individual buffer's duration (external corroboration ml-explore/mlx#3267: ~1.2 s single ops killed display-on, MLX_MAX_OPS_PER_BUFFER=1 powerless). The rung-0 "35 packed dispatches killed" = ~4-6-dispatch ~1.0-1.5 s PACKED BUFFERS (scalar-fwd small-output row) — per-buffer explains every kill AND survival on record. Behavioral confirmation on this wheel: ops-knob discriminator 2.8× (0.018→0.051 s); size-knob directional; **consequence probe: 12-dispatch chained dK/dV at N=12,288 (nominal projection ~4.3 s = the refused class) ran clean in 0.781 s, finite, 1.56 GB peak** (`scripts/probe_command_buffer_packing.py`, artifacts `_artifacts/launch_budget_evidence/`, research artifact `docs/superpowers/research/2026-07-14-mlx-train-perf-launch-budget-evidence.md`). CONSEQUENCE (spec §3 amended): C0 = packing-aware per-BUFFER projection at UNCHANGED MAX_DISPATCH_SECONDS 0.5 + causal-true (exact triangle) MAC accounting; per-chain MAX_TOTAL_SECONDS retired for attention launchers; fwd range sizing must keep even a maximally-packed buffer ≤ budget (closes a LATENT old-guard gap: packed scalar-fwd buffers reached ~2 s projected legally). Launch-side ceiling DISSOLVES → North-Star ceiling becomes memory-bound; the 0.2.0 "RAM-independent cap / big-RAM inversion" caveat dies with the mechanism (docs must state the correction). C1 (dkv per-ct interleave) demoted to throughput rung, same graduation bar. user-metal-kernels updated same-day (buffer-composition rule + kill-unit entry rewritten; eval-level interpretation retired, raw artifacts kept). Worst-day doctrine intact — NOTHING raised. NEXT: T2/T3 (C0 cost model TDD + launcher wiring), then C1 rung, riders (0016/0023/0022c-f), code-freeze, campaign (northstar re-sweep — expect memory-bound convergence well past 10,240), release endgame.

# ═══ 0.2.0 (started 2026-07-09) ═══

- 0.2.0 SHIPPED (2026-07-14 ~08:50 CEST): PR #2 merged (rebase, tip 37660d7) after an 11-test CI fix (0214bdb — 7 GB runners trip the guards safe-start floor; in-process tests fake the ceiling, real-subprocess tests skipif; one gc-under-compile peak-comparison flake rerun-green → backlog 0023) + hotfix PR #3 (ae45024 — the tag gate flagged uv's dist/.gitignore; now judges only .whl/.tar.gz; end-to-end-verified against a real build this time). Tag v0.2.0 re-created at ae45024 (first tag deleted unpublished after its own gate correctly skipped publish), pushed with Denis's authorization → publish workflow ALL GREEN (build+gate, metal-tests, publish) → PyPI serves 0.2.0 (wheel+sdist). GitHub topics +flash-attention/memory-optimization/llm-training/long-context (20/20). Repo CLAUDE.md refreshed (shipped state + gotchas 19-21: calibration pollution, lid-close vs caffeinate, availability-metric mechanics). Backlog: 0018 done, 0015/0020/0021 done, 0023 filed. REMAINING (minor, next session): 0018 Done-table row + public-ROADMAP cross-check already shipped in-release; local branches dev/v0.2.0 + fix/publish-gate-uv-gitignore await gated force-delete (rebase-merged, cherry-verify first); EXAMPLES.md rule does not apply (no EXAMPLES.md in this repo).
- T18 RELEASE PR OPENED (2026-07-14 ~00:15): campaign CLOSED. Llama flash arm fresh (0.92x@2048 — draft 1.003x did NOT reproduce; 0.97x@8192 ✓). The fused+stock-attn@8192 cell was attempted 4x (pulse variants incl. MLX-buffer free-pool inflation) — physics verdict: Denis's floor state caps avail at ~26.6 GiB → ceiling ~24.6 < the ~25.4 peak; watchdog aborted honestly every time (final artifact: abort at ceiling 24.54, observed 25.42). Denis's ruling: run within current limits accepted → REWORD path: throughput paragraph rewritten around the three fresh pairs (5.3%@8192 stock-loss, 5.5%/5.9%@2048) + the unpaired baseline stated as an honest abort ("the condition flash exists to remove"); humanizer-audited. Loss-parity claim survives verbatim (fresh worst 2.36e-3 = Llama@2048, rounds to the drafted 2.4e-3). Worst attention-arm ratio now 0.94x (was stale 0.898x). Dates re-set to 2026-07-14. GATES ALL GREEN: metal lane 1368 passed/4 gated skips + cov, ruff, mypy, uv build + sdist/wheel leak audit, both public-docs leak greps. Release commit (README/CHANGELOG/ROADMAP/community row json) + push + PR via bash-git-ops — STOPPED at the PR for Denis's merge. PINS AT PR: 0.85x floor (fresh worst 0.94x), "near-tied" at 10240 vs 9728, Llama 0.92x shift, dropped 4.0% fused@8192 claim, date 2026-07-14. Tag push = separate authorization after merge. NEW-GOTCHA candidates for repo CLAUDE.md post-merge: crowded-machine calibration pollution; lid-close kills caffeinate; free+inactive+speculative metric mechanics (pulses can't reclaim live active sets; MLX child teardown inflates the pool for the NEXT process).
- T18 EVIDENCE REFRESH — CAMPAIGN RESULTS (2026-07-13): fresh artifacts at release code_sha, all measured through the committed scripts. VERDICT: the drafted story reproduces; several numbers move slightly. attention-op (clean, warning-free): fwd 0.186444 / bwd 0.576229 / 3.09x (docs updated 0.183→0.186, 0.575→0.576); ratios flash 2.00x/3.06x, stock 3.76x ✓; stock@8192 honest abort ✓; flash@16384 refusal ✓. train-step flash arm (all ok): 8192 fused 12.746/8.374 GiB EXACT vs draft, 72.9 tok/s; fused-vs-stock CE under flash 0.94x@2048 (86.4/92.1), 0.99x@8192 (72.9/74.0) — draft's 1.05x@8192 did NOT reproduce (was 0.985 fresh); Llama arm re-running (stale 14cdad93 — caught as scope gap). stock-attention arm: 8192 stock-CE 25.6808/21.3082 @ 78.1 tok/s EXACT vs draft, clean; 8192 fused-CE ABORTED at dynamic ceiling 24.41 (avail dipped between conditions) → single-condition re-run gated overnight (avail ≥27.5 GiB, chain3). northstar: ours max-fit 10240 (draft 9984 — healthy-state calibration lifted the launch-budget cap one notch; refusals all launch-budget ✓), stock 9728 ✓ aborts at ceiling 28.0 ✓ EXACT reference semantics (cache-inflation ordering made the static term bind). Kit row: quick tier clean, no warnings → community-benchmarks/apple-m1-max-32gb-2026-07-13.json + README table row pasted. Loss parity fresh worst 1.82e-3 (draft 2.4e-3; final recompute pending Llama+cell). OPERATIONAL LEARNINGS: (1) crowded machine pollutes construction-time rate calibration (80.1 vs 695 G MAC/s → launch-budget refusals at 8192 even when memory fits); (2) closed-lid sleep freezes runs THROUGH caffeinate (battery night lost); (3) availability metric = free+inactive+speculative — model-load file cache inflates it in-sweep (that is HOW the reference got ceiling 28.0; northstar-first ordering exploits it legitimately); (4) allocation pulses don't move the metric when free is plentiful. DOCS: settled numbers edited (README/CHANGELOG: split walls, 9984→10240, 14.8 GiB @10240 peak, community row). PENDING before release commit: Llama ratios + fused+stock-attn@8192 cell → rewrite the throughput paragraph (4.0%/74.9, 10.2%→5.3%, 6.4/6.1→5.6/5.8, 2.4e-3, 0.898x-worst all in one pass) in README:45 + CHANGELOG:34-38 → dates on CHANGELOG/ROADMAP 0.2.0 headers → full gates (metal+cov, ruff, mypy, build+leak) → release commit (3 docs + kit row json) → push+PR+STOP (pins: 0.85x floor, headline numbers, "near-tied" at 10240 vs 9728).
- T18 RC-REVIEW ROUND + FULL EVIDENCE REFRESH (2026-07-12 ~17:30-…): Denis interrupted the resume with an external RC engineering review (docs/superpowers/reviews/2026-07-12-mlx-train-perf-0.2.0-release-candidate-review.md, 4 findings) — ALL 4 verified true against the repo: (1) High — every headline artifact stale under our own code_sha contract (current 6f6fd96b244d4bb1 vs 14cdad93/b0e89841 train-step/northstar and 63e1f001 attention-op; cause = post-campaign watchdog commits touching worker.py/guards.py, math unchanged); (2) fwd/bwd 0.183/0.575/3.1x split not recorded by the cited script; (3) ROADMAP "cap lifts on larger machines" + CHANGELOG "context lever grows on larger machines" contradict README's correct RAM-independent-cap statement; (4) publish.yml publishes any v* tag with NO test gate and NO tag↔version assertion. DENIS'S RULINGS (AskUserQuestion, ~17:35): full fresh re-run of ALL headline benches post-fixer (not docs-narrowing); publish gate fixed IN THIS PR; split claim KEPT via new timing fields + regeneration. EXECUTED SO FAR: consolidated fixer re-dispatched with amendments (item 2 superseded, item 9 = finding 3) → commit fe24d4b (9 items, gates green, docs still uncommitted); bench decomposition fields TDD'd (pure decompose_walls + rep-timed fwd window; artifact gains fwd_wall_s/fwd_walls_s/bwd_wall_s/bwd_over_fwd; aggregate_community reads only fwdbwd_peak_gb — additive-safe) → cda7d4c; publish gate (stdlib-only scripts/assert_tag_version.py, 14 tests + metal-tests job in publish.yml, publish needs [build, metal-tests]) → 5297ca5. Old artifacts preserved at _artifacts/pre-rc-refresh-backup-2026-07-12/ (the pre-net 32.4 GB stock@8192 evidence lives there — post-net re-runs abort that condition by design). CAMPAIGN (runbook: .superpowers/sdd/task-18-rc-campaign-runbook.md; logs _artifacts/campaign_logs/): (a) attention-op grid + flash@16384 refusal LAUNCHED ~18:05 background; then (b) train-step flash arm, (c) train-step stock arm (SEPARATE --out per arm, gotcha 18; one invocation per arm covers both loss arms via _ARMS), (d) northstar sweep, (e) Trash crowded kit JSON + quiet-machine contribute row. Then number-refresh vs FRESH artifacts → full gates → release commit → push+PR → STOP (Denis pins 0.85x floor + headline numbers at the PR).
- T18 FINAL FAN-OUT VERDICTS (2026-07-12, 7 lanes): release-gate GO 8/8; code Approved (4 minors → 0022 w/ prescriptions); mlx CLEAN (5 seams); tests SOUND (0 High/Med; PR wording note: default lane = no-JIT/no-model, not no-Metal); docs ship-ready EXCEPT reachable-docstring tracker leaks (contribute/attention-init/api Medium); perf-claims CONDITIONAL GO (B1/B2: stock-attn@8192 artifacts overwritten, values ledger-only → RE-RUN pair required pre-tag; B3 wrong script cited for fwd/bwd split); fable ship-with-fixes (CAUGHT: watchdog makes stock@8192 single-op reproducer promise structurally false — reframe as pre-net hazard measurement; 0.85 floor = bar not mechanism; calibration provenance regression). CONSOLIDATED FIXER dispatched (opus, 8 items, docs+docstrings+json-metadata only). REMAINING: fixer → GPU restores on idle machine (stock-attn@8192 both arms ~60-75 min + kit row ~1 min) → full gates → release commit (docs+row) → push+PR → STOP (Denis pins floor+numbers). Fable audit: docs/superpowers/reviews/2026-07-12-mlx-train-perf-0.2.0-fable-audit.md.

Plan: /Users/ionden/Documents/Work/mac/docs/superpowers/plans/2026-07-09-mlx-train-perf-0.2.0-plan.md (19 tasks; T9b inserted by the plan-review round)
Spec: /Users/ionden/Documents/Work/mac/docs/superpowers/specs/2026-07-09-mlx-train-perf-0.2.0-design.md
Branch: dev/v0.2.0. Controller tasks: T1, T2, T6, T9b, T13, T16-step2, T17, T18.
Plan review round 2026-07-09: 4-specialist fan-out (solution-architect / review-mlx / review-code / review-tests), 2 blockers + ~30 findings ALL folded into the plan before execution (T10 forward-ref fix, NEW T9b backward-timing gate, T14 residual-OLS fit redesign, T15 constraints-not-formula, T5/T8 splitting, gc=True subprocess isolation, T2 observable-mechanism fix, FD gradient anchor, forced-chaining oracle test, and the rest — see the plan's "Plan review round" header note).

- T12 carry-forward (1) FIXED (controller, commit 1c10ed3): drift lane now pins create_attention_mask's "causal" (N>1) and None (N==1) sentinels; falsifiability proven by perturbation (WRONG-SENTINEL → FAILED → restored → green). Carry-forwards (2) batch_size-hint prominence and (3) qwen3 arm → folded into T13 (worker passes hints matching training shapes — in the Step-1 dispatch; qwen3 = the Step-3 8B campaign model).
- **TASK 17 COMPLETE (2026-07-12, controller):** wheel smoke ALL PASS in a clean py3.11 venv (uv build 0.1.1.dev34+g343d1ce4b): mlx version bounded ✓, package imports ✓, loss auto tiny call finite ✓, flash_attention kernel tiny fp32 ✓, enable_flash_attention tiny-llama forward ✓, console script resolves ✓, **_bench_scripts_dir resolves INSIDE the installed wheel** (T16's force-include design works — the reviewer carry-forward closed). Leak audit: sdist carries only the intended community README; wheel only the two intended _bench_scripts; no CLAUDE/superpowers/artifacts/codegraph. No packaging gaps → no commit. (Two smoke-script iterations were script bugs — DenseHead API + venv PATH — not packaging.) NEXT: T18 (release docs + review fan-out + PR STOP; needs the idle-machine kit re-run for the first community row + Denis's floor/headline pins; backlog 0020's review artifact still open — fold into the T18 window).
- **TASK 16 COMPLETE (2026-07-12):** round-2 fixes landed (commit 343d1ce — stale-marker unlink before respawn verified in source w/ rationale comment; per-bench progress lines [i/N + ETA label + status]; courtesy stdout-tail consciously deferred-documented; 125 target + 715 default green) + controller spot-check in lieu of a 4th review cycle (T18 whole-branch covers). **Step 2 REAL RUN (log _artifacts/campaign_logs/t16-step2-quick-tier.log): the full crowded-machine stack fired IN THE WILD on first use — divergence warning BEFORE confirmation (measured 16.76 GB vs expected 30.06), run proceeded under the dynamic 14.6 GB ceiling, stock@8192 honestly aborted_memory_ceiling (watchdog's 4th real firing), 14/15 conditions ok, artifact provenance-complete (machine block + memory_warnings + statuses), aggregator renders the row.** Wall ~1 min vs printed 10-15 min ETA (safe direction, ~10× over-wide — T18 nit). Table nit: "flash 2x ratio 3.06" presents the split-regime doubling without context (show both doublings or footnote — T18). Crowded-run artifact kept UNCOMMITTED as validation evidence; official first row = idle-machine re-run (~1 min) at T18 commit time. NEXT: T17 wheel smoke (controller).
- T16 fix-round re-review (sonnet review-code): A/C/D/E FIXED clean (ordering test genuinely discriminates; aggregator loud-skips pinned; session-id scope traced safe — measured-path staleness independently covered by code_sha/script_sha; exit-2 policy). B core fix good BUT introduced a REPRODUCED regression: fixed-name _spawn_crashed.json never superseded in the now-deterministic work dir ⇒ permanent phantom "error" in every later community artifact after one transient crash; PLUS the script arm regressed to fully-captured silence (zero kit-level progress for a 1-2h run). Smoke declared SAFE (residuals don't affect a fresh single invocation) but BOTH residuals go to fix round 2 (sonnet, dispatched) before the kit meets contributors. Round-2 scope: unlink stale marker before respawn; kit-level per-bench progress lines (keep capture for crash honesty).
- T16 fix round IMPLEMENTED (commit 3785a8b, sonnet — the first opus fixer died on the session limit mid-read, prescriptions re-dispatched verbatim per the handoff pattern; scoped re-review IN FLIGHT sonnet review-code, package review-7004afc..3785a8b.diff): A pre-flight-before-confirm (README promise now true, unedited), B crash envelopes for artifact-less script failures (tradeoff disclosed: captured not live subprocess output, runner convention), C loud aggregator skips, D stable recipe-derived session id (cross-invocation resume restored), E exit-code policy. 122 target + 712 default green, all gates clean.
- Task 16 review: **Needs-work** (opus review-code; all flag surfaces traced valid, no forked measurement code, claims envelope clean, gates reproduce — but 1 High + 3 Medium contributor-protection gaps): [A High] pre-flight warnings print AFTER measurement + confirmation happens BEFORE pre-flight (contradicts community README's warn-and-retry promise — a crowded machine burns the hour then learns it was crowded); [B Med] _spawn_script ignores exit codes — whole-script crash with empty glob recorded as clean empty bench; [C Med] aggregator skips malformed files silently + renders partial files as garbage rows; [D Med] fresh session_id per invocation defeats cross-invocation resume for 3/4 benches; [E Low] sysctl failure escapes exit-code policy. FIX ROUND DISPATCHED (opus, prescriptions verbatim). Step-2 smoke carry-forwards recorded: idle+AC+green pressure required (the e2e test uses REAL readers); watch wall vs the printed ETA; verify artifact provenance + aggregator row; T18 gate: _bench_scripts_dir wheel-resolution needs the clean-env check.
- Task 16 code IMPLEMENTED (commit 7004afc — amended from 9089cbb to include tests/test_cli.py after the implementer's git child correctly REFUSED a peer amend-instruction contradicting its explicit file list and escalated to the controller; controller verified the diff kit-only and authorized. 10 files, +1718: contribute.py 628 lines, aggregate_community.py, community-benchmarks/README, CLI subcommand, README stub, 3 test files. Review IN FLIGHT opus review-code, package review-1717b07..7004afc.diff — honesty surfaces (ETA-before-work, TTY/--yes, red-pressure refusal, 0021 divergence-warning surfacing), no-new-measurement-code check, scaling-table small/big-end sanity vs the 0021 refusal + launch-budget zones, provenance verbatim-identity + no-hand-editing, claims envelope vs T14 constraint.) Controller Step 2 (real quick-tier smoke, ~10-15 min GPU) runs after review approval.
- Task 14: complete (commit 1717b07, review ✅ Approved opus review-code — Spec PASS, zero blocking. Fit reproduced independently byte-for-byte (a_flash 18633.411713264017 from the two _ours FitPoints; base/a_lin genuinely held); analytic O/L term CONFIRMED against measurement (hand-computed 0.0159 GB == measured fwd_peak exactly); q-head driver consistent across fit/branch/artifacts; stock path value-level byte-unchanged; crossover honest (~2142, no "always less" claims). CUSHION CRUX ADJUDICATED: OLS + global ×1.10 is correct — the flagship −1.6% raw under-fit is aggregate-model (max-residual refit only buys ~0.13 GB, still under), every sub-anchor point over-predicts raw, and term-level margin would break methodology consistency with the stock a_quad fit. Lows: pre-0.2.0 calibration file KeyError (dev tooling); LATENT load_fit_points contamination edge (stock-bool arm not filtered — shipped fit provably clean from _ours files; whole-branch review item: refuse stock-bool artifacts); provenance nit (machine tag downgraded); Trash-vs-rename housekeeping. T18 DOCS CONSTRAINT: planner claims = over-predicts CUSHIONED TOTAL at all 4 anchors within the ≤8192 measured envelope, NEVER the marginal, NEVER a context ceiling (northstar owns that); >8192 refit = 0.2.x follow-up.)
- Task 14 IMPLEMENTED (commit 1717b07, review IN FLIGHT opus review-code, package review-7708388..1717b07.diff): driver form = analytic O/L saved state + a_flash·batch·heads·seq (single-op 2048→4096 exactly 2.00×; 4096→8192 excess = exactly 4.0 chained generations, folded into the slope — no second free param over 2 anchors); **a_flash = 18633.41 B/(head·tok)** residual-OLS, base/a_lin held; over-predict-safe all 4 anchors (Qwen +16.5/+8.2%, Llama validation +19.9/+15.4%, tol<20% pinned); stock path untouched. 16 RED-first tests, 83 target/623 default green, ruff+mypy clean. Concerns → review adjudication: Qwen-8192 margin leans on the ×1.10 cushion (raw −1.5%) — THE review crux; no anchors >8192 (extrapolation conservative; northstar remains the context gate); crossover ~2138 disclosed (flash O(N) > tiny O(N²) at short seq — physics); 0.1.0 report went to Trash not rename-in-place (housekeeping note). STOP pins (floor + release numbers) STILL OPEN with Denis.
- Task 14 DISPATCHED (2026-07-11, opus, BASE 7708388, brief task-14-brief.md): planner flash branch + a_flash residual-OLS (base/a_lin held fixed per the rank-deficiency ruling), driver form settled from the measured single-op scaling (linear + budget-bounded split-constant mechanism handed over), Llama-3B = cross-model validation. NO GPU (fits existing T13 artifacts; BLOCKED-with-command if a new measurement is truly needed). NOTE: STOP-table pins (step-time floor + release numbers) still formally OPEN with Denis.
- BACKLOG 0021 COMPLETE (commit 7708388, review ✅ Approved opus review-mlx — COMPLIANT, zero blocking. Ladder recomputed byte-exact incl. 32→28.0 preservation + monotone-at-anchor; parser sums exactly free+inactive+speculative (NO purgeable double-count), header page size honored, all failure modes→None; garbage-low availability REFUSES typed (never installs a ≤0 ceiling), garbage-high caps at static; refusal MemoryBudgetError package-rooted; memory_warning = top-level result field, NO identity/schema drift, resume-collapse correct; watchdog/wall/wired/breach byte-unchanged (diff-verified); distributed posture accurate, no mx.distributed/global state. ADJUDICATIONS: WorkerCrashed envelope for the too-crowded refusal CORRECT for 0.2.0 (environment-transient ≠ condition-intrinsic "refused"; distinct status = 0.2.x polish); default-path coverage sound; warning-on-refused correct. THREE Lows for the final review: dead None-arm ternary in the refusal message; single-sample availability (by-design, operating-notes item); missing refused-path memory_warning assertion (one-liner). CARRY-FORWARDS: T18 docs must describe the two-term ceiling + refusal + clean-machine pre-flight advice; >32 GiB reserves are MODEL not calibration — first big-RAM run confirms or triggers the 3-part review.) Backlog 0021 acceptance boxes updated.
- BACKLOG 0021 IMPLEMENTED (commit 7708388, review IN FLIGHT opus review-mlx, package review-9771287..7708388.diff): static anchored ladder (32→EXACTLY 28.0 pin; exact-byte pins 64→62706522521 … 1024→1041959066009), parse_vm_stat (header page size, real fixture + 5 malformed→None), available_memory_bytes degrades to None→static-fallback+warn, effective=min(static, avail−2 GiB), MemoryBudgetError refusal < mem//4, expected-free divergence warning (0.75 overridable) logged + memory_warning artifact field (omit-when-None), cluster posture documented (rank-local inputs; launcher propagates rank death). Denis refinements + web-calibration + distributed notes all folded (three dispatch messages, attributed). 147 targeted / 608 default green, ruff+mypy clean, RED-first. Implementer concerns for the review to adjudicate: refusal surfaces as WorkerCrashed envelope not a distinct artifact status; 0.75/2 GiB constants validated only at the 32 GB baseline (first big-RAM campaign sanity-checks); memory_warning also on refused artifacts.
- BACKLOG 0021 DISPATCHED (2026-07-11, Denis pre-ship ruling, opus implementer): device-relative + DYNAMIC memory ceilings — static rule T15-anchored at the 32 GiB baseline (32→EXACTLY 28.0 preservation pin; <32 → mem−4 GiB floor mem//2; >32 → 28 + 0.95·excess, ladder pinned 64→58.4/128→119.2/512→484/1024→970.4), dynamic term = vm_stat-measured availability at start − 2 GiB (anchored by the fresh-boot baseline: 29 avail ⇒ ≥26.5 > 25.68 legit), effective = min(static, dynamic), REFUSAL below mem//4 (typed error, no silent degradation), reader failures fall back static+warn. No new deps (vm_stat parse, pure parser + injected reader). Wall/wired/breach semantics untouched. Item 0021 filed + indexed.
- **T13 Step 4 NORTHSTAR DONE (2026-07-11 ~15:xx, converged both arms, artifacts _artifacts/northstar_context_sweep/): ours (flash+fused-CE) max-fit 9984 / min-fail 10240; stock max-fit 9728 / min-fail 9984.** THE MECHANISM SPLIT: every failing OURS probe = status REFUSED (LaunchBudgetError: dkv chain total crosses MAX_TOTAL_SECONDS 2.0 at ~824G session rate) — **ours' ceiling is the WORST-DAY LAUNCH BUDGET, not memory** (peak at 10k ≈ 13-14 GB ≪ 28 ceiling); every failing STOCK probe = status aborted_memory_ceiling at 28.0 GiB — **the WATCHDOG FIRE PATH IS REAL-WORLD VALIDATED** (multiple honest aborts, no panic, sweep continued reading them as does-not-fit; backlog 0020's re-armed-probe box satisfied by campaign evidence). Two transient WorkerCrashed probes (8704_ours, 8960_stock) with successes above — search converged past them, footnote for final review. HONEST CAVEATS for the STOP/release: (a) NOT comparable to 0.1.0's ~8450/8700 numbers (measured quantity + watchdog semantics changed, pre-registered); (b) ours' budget cap is rate-derived, RAM-independent — on a 64+ GB machine stock's memory ceiling rises while ours' ~10k budget cap stays (until faster dkv kernels or new kill-evidence raise it) — the context-win narrative INVERTS on big-RAM boxes, must be stated; (c) levers to raise ours' cap: dkv rate tuning (0.2.x), kill-active-day budget evidence, non-compiled chain flushing. Step 5 STOP presented to Denis.
- T13 Steps 3-restore + 3b DONE (2026-07-11): stock-attn@2048 pair restored into _artifacts/bench_train_step_stockattn/ (ours 91.387 / stock-loss 97.776, ratio 0.9347) — attention cost now complete: flash −6.4%/−6.1% tok/s at 2048 (ours/stock-loss arms), −4.0%/−10.2% at 8192. Step 3b Llama-3.2-3B flash (artifacts _artifacts/bench_train_step_flash_llama3b/): 2048 ours 187.47 vs 186.829 (1.0034×); 8192 148.261 vs 152.346 (0.9732×); loss diffs 2.4e-3/9.9e-4 — T14 cross-model input BANKED. Step 4 NORTHSTAR LAUNCHED (~13:3x, --arm both, bf16, gc, log _artifacts/campaign_logs/step4-northstar.log, ETA 1-2h, watchdog under every probe).
- T13 Step 3 FLASH DONE (2026-07-11 ~12:5x, 4/4 conditions ok, log _artifacts/campaign_logs/step3-flash.log): **Qwen3-8B gc=True HEADLINE — flash attention at seq 8192: total peak 12.7462 GiB vs stock-attn 25.6808 = HALF the peak (marginal 8.3736 vs 21.3082 = 2.55× less), at −4% tok/s on the kernel-loss arm (71.86 vs 74.881); stock-loss arm 68.473 vs 76.279 (−10%). Loss curves match stock-attention runs to bf16 class (12.849/12.759/12.558 ≈ identical). Loss-arm ratios under flash: 2048 ours/stock 0.9312 (the familiar ~7% kernel-CE cost); 8192 ours/stock 1.0495 — kernel CE FASTER than stock CE at 8192.** FINDING (controller miss): condition names lack the attention arm + shared out-dir ⇒ the flash invocation OVERWROTE last night's stock-attention artifacts (identity differs, filename doesn't; pre-spawn unlink). 8192 stock-attn numbers preserved in this ledger; 2048 stock-attn pair LOST → cheap re-run queued into _artifacts/bench_train_step_stockattn/ (~15 min). Fix for final review: condition_name should carry the attention arm (or campaign discipline: one out-dir per arm). NEXT: stock-attn@2048 restore → 3b Llama-3B flash → northstar.
- T13 RELAUNCH (Denis approved 2026-07-11 ~10:5x): fire-path run FIRST — **the killer condition (stock-attn/stock-CE@8192) COMPLETED CLEANLY on the fresh-boot machine** (artifact 11:31: ok, 76.279 tok/s, total peak 25.6808 GiB < 28.0 ceiling, 20 steps, losses healthy). Readings: (1) the panic was SYSTEM-STATE-driven (last night: hours of prior conditions + Step-2's 32.4 GB pager pre-degraded swap/compressor), not schedule-driven — same schedule fits on a fresh boot; (2) watchdog NO-FALSE-POSITIVE validated at the near-cap class (25.68 < 28.0, untouched); (3) fire path itself remains injected-only-proven (nothing breached today — fine); (4) stock@8192 peaks IDENTICAL across ours/stock loss arms (25.6808 both) — peak is attention-dominated at 8192, loss-layer delta absorbed by reuse — the flash arms are where the story changes; (5) bench summary prints pair-status "missing" when --condition runs one arm against a stale-session sibling — cosmetic, note for the final review. Step 3 FLASH invocation launched (~1-1.5h ETA), log _artifacts/campaign_logs/step3-flash.log. Then 3b, northstar, STOP.
- WATCHDOG COMPLETE — SAFETY GATE CLOSED (fix commit 9771287, re-review ✅ Approved opus, zero findings): C1 exit-always verified on every path (production os._exit never returns ⇒ no re-raise point; _watchdog_step suppress contains the test-only propagation both ways); H1 run_grid byte-mirrors runner.py's two-branch semantics; W=7200 pinned with rationale. RED evidence included genuine stash/revert/rerun. 6 files +118/−20, gates reproduce (102 passed targeted, ruff, mypy). Hardening chain: ee3f311 → 9771287, two opus safety reviews. Backlog 0020 acceptance box 1 (watchdog merged) DONE; remaining: review artifact + prior-art research + re-armed bounded probe + roadmap upkeep. **CAMPAIGN RELAUNCH: code-ready, HELD for Denis's explicit go** (remaining: Step 3 flash invocation + stock-CE@8192 re-run [expected honest abort ≈ the datum] + Step 3b Llama-3B + Step 4 northstar; first heavy run = real fire-path validation).
- WATCHDOG safety review (opus review-mlx): **Needs-work — [Critical] C1: hard exit NOT guaranteed (write_result before exit_fn; a storm-regime MemoryError in the artifact write is swallowed by _watchdog_step's suppress → watchdog DISARMS ITSELF; reviewer proved it — "HARD-EXIT SKIPPED"); fix = try/finally exit-always.** [High] H1: run_grid clobbers breach artifacts (missing the `and not out_path.exists()` guard runner.py has). [Medium] missing C1-path test. VERIFIED SOUND: ceiling accounting apples-to-apples (get_active_memory and total_peak both = active-excluding-cache ⇒ real 2.32 GiB margin over the 25.68 legit peak, 4 GiB under RAM); GIL fire-path proven empirically (172 daemon samples during a 517 ms eval); northstar semantics correct (breach → rc 70 + artifact → "does not fit", no sweep abort). Wall default adjudicated too tight (3600 vs legit 3300s) → raise to 7200 + explicit budgets on long conditions. Relaunch carry-forwards: fix C1+H1 first; expect re-measured ceilings to land slightly LOWER (a pre-watchdog "fit" at 28-32 GiB active now honestly aborts — a correction, not a regression); first heavy run = the real fire-path validation. FIX ROUND DISPATCHED (sonnet, reviewer's verbatim prescriptions).
- WATCHDOG IMPLEMENTED (commit ee3f311, safety review PACKAGE review-7e611f0..ee3f311.diff): memory_ceiling_bytes = max(memory_size − 4 GiB, memory_size//2) = 28.0 GiB here; injectable sampler/clock; pure breach decisions; make_watchdog_on_breach (artifacts.py) writes aborted_memory_ceiling/aborted_wall_budget via write_result then os._exit(70); runner respects worker-written artifacts on nonzero exit (pre-spawn unlink guarantees provenance); wall_budget_s rides the 7e611f0 top-level seam, NOT identity (safety limit ≠ measurement dimension, no SCHEMA bump), None→3600s default; installed in worker.main + bench_attention_op child. 8 files +817/−44, 100% cov on guards/artifacts/runner, RED-first, default 579, ruff+mypy clean. Backlog 0020 filed P0 in-progress + roadmap row; doc corrections landed in repo CLAUDE.md (gotcha 16), workspace CLAUDE.md (amended "let it run is safe"), global CLAUDE.md §D (amended), user-metal-kernels (paging-storm panic entry). Review crux assigned: active-vs-total accounting for the 2.3 GiB ceiling margin; wall default 3600 vs the known-legit 55-min condition. RELAUNCH still HELD for Denis.
- **T13 Step 3 KERNEL PANIC (2026-07-10 ~23:55, machine down, rebooted 23:56; diagnosed 2026-07-11 morning):** panic-full-2026-07-11-084736: "completeMemory() prepare count underflow" @IOGPUMemory.cpp:550, panicked task python3.13 pid 54908 = the seq8192_STOCK-CE worker (stock attention + stock loss, Qwen3-8B gc=True). Timeline: 2048 conditions ok 19:50/19:57; 8192_ours ok 20:58 (legit-slow ~75 tok/s, total peak 25.68 GB); 8192_stock = 25.7 GB class + (N,V) logits head → past 32 GB → ~3h GPU paging storm → IOGPU driver assertion panic. Flash invocation never ran. /tmp logs lost to reboot (campaign logs must tee under _artifacts/ henceforth). **SAFETY MODEL DISPROVEN (3-part-review trigger, backlog item to file): "over-budget → clean MLX OOM, never a panic" is FALSE on mlx 0.32.0/macOS 26.5 — set_memory_limit is advisory (docstring: raises only when RAM+swap exhausted); allocations past the soft cap PAGE (Step 2: 32.4 GB marginal ran); a sustained GPU paging storm panics the kernel via IOGPU. The wired cap covers ONLY the wired class.** Controller accountability note: Step 2's own 32.4 GB stock@8192 single-op reading should have forced a re-projection of Step 3's stock-CE@8192 condition before launch — fresh same-session evidence beats 0.1.0 priors. HARDENING dispatched: active-memory ceiling watchdog (memory_size − 4 GiB = 28.4 GB here; above legit 25.7, below storm 32+) + per-condition wall backstop, honest abort artifacts, in worker.main + bench_attention_op child. GPU relaunch HELD for Denis's explicit go.
- T13 Step 2 DONE (2026-07-10 ~20:05, artifacts _artifacts/bench_attention_op/, 7 conditions): **flash fwd 2.00×/2.00× doublings (pure O(N)); flash fwd+bwd 2.00× then 3.06×; stock 3.76× (≈ the 3.8 spec baseline) then 3.05×-compressed-by-PAGING (32.4 GB marginal at 8192 EXCEEDED physical RAM — wired cap prevents panic not allocation; wall 18.3s vs 0.45s = thrash; the T11-review OOM expectation was wrong in an interesting way). Flash absolute: 0.72 GB vs stock 32.4 GB at 8192 = 45× less. Flash 16384 = honest budget REFUSAL artifact.** Flash 3.06× anomaly MECHANISM CONFIRMED same-session by pure arithmetic: dkv chain = 1 range at 2048/4096 but 3 ranges at 8192; retained chained dk/dv generations under the single backward eval = 0.25 GB ≈ the exact excess (0.72 − 2×0.235). Budget-bounded (total≤2s caps ranges ≤~4) ⇒ documented constant, not runaway; BACKLOG candidate: mid-chain buffer donation. STOP-table framing: O(N) un-split; split adds ≤~0.3 GB bounded overhead. Step 3 LAUNCHED (Qwen3-8B, seq 2048+8192, stock+flash attention invocations, ETA 30-60 min).
- T13 SMOKE PASSED (2026-07-10 ~19:50, artifacts _artifacts/bench_train_step_flash_smoke/): real 3B flash run end-to-end clean — both loss arms ok, identity carries attention_impl=flash, losses finite+decreasing (12.42→12.18→12.04 = LoRA alive inside the wrapper), loss-curve worst diff 0.0036 (bf16-class), prewarm under cap, no refusals. (ours/stock tok/s 0.72 at seq512 = loss-layer-dominated tiny-seq regime, not a campaign number.) Step 2 single-op scaling LAUNCHED (~10-20 min ETA): N 2048/4096/8192 both arms + 16384 flash-only (expected: stock-8192 clean OOM artifact; flash-16384 budget-refusal artifact — findings, not faults).
- T13 seam fix: complete (commit 7e611f0, scoped re-review ✅ Approved opus — COMPLIANT, zero findings. Seam single-pathed (grep-verified; only guard-assertion test hits remain); RESUME TRACE verified: runner freshness check and worker artifact write compute identical identities ("flash" both sides after JSON round-trip; omitted-when-None symmetric) ⇒ flash artifacts resume correctly incl. northstar's stable-session path. Pre-fix properties survived (ordering/hints/once-per-process/guardrails-first/per-arm forcing/stock byte-unchanged). Smoke checklist for the campaign opener: real gates e2e; LoRA-inside-wrapper tell = moving loss tracking stock; prewarm full-shape forward must stay under the wired cap at long context (it's live wired memory though excluded from marginal peak); stock arm should reproduce 0.1.0 numbers.)
- T13 seam FIX IMPLEMENTED (commit 7e611f0, scoped re-review IN FLIGHT, package review-012fadb..7e611f0.diff): first-class attention_impl (runner.Condition field → top-level worker-config JSON key → worker.main → run_train_step kwarg; params read REMOVED — one path). 7 regression tests RED-first (2 with the exact production BenchInputError; worker KeyError); 0.1.0 identity keys pinned byte-identical when unset; stock≠flash identities pinned. Default lane 550, targeted 120, ruff+mypy clean. Fixer notes: script_sha rotation of bench_train_step/northstar = correct (no attention-unaware session resumable); dkv_split_policy/attention_variant remain identity-only kwargs without Condition slots (fine — only bench_attention_op uses them, own machinery; first-class them if a train_step condition ever needs them).
- T13 SMOKE FINDING (2026-07-10 ~19:25, Critical, campaign-blocking): the real-model flash smoke CRASHED at runner.condition_identity — BenchInputError "params must not use the reserved key 'attention_impl'". Step 1 threaded attention_impl through PARAMS in bench_train_step (and northstar build_probe line 121 does the same), but T10's hijack guard reserves it as a DEDICATED identity kwarg (T11's script passes it correctly as attention_impl=). The 9 RED-first tests AND the opus review both missed it — the tests stubbed below the runner/identity seam. Fix dispatched (opus): first-class attention_impl through runner.Condition + worker.main + both scripts + a regression test that exercises the REAL identity construction. The smoke did exactly its job.
- Task 13 Step 1: complete (commit 012fadb, review ✅ Approved opus review-mlx re-dispatch after 7pm reset — COMPLIANT + APPROVED, zero blocking findings. Worker ordering VERIFIED AIRTIGHT line-by-line (load→cast→eval(params)→enable(hints)→freeze→LoRA→train, worker.py:339-397); hints traced EXACT to the compiled shapes (batch/seq/n-bucket AND dtype — prewarm reads post-cast norm.weight.dtype); enable once-per-process; guardrails-first cap ordering → prewarm OOMs cleanly; prewarm arrays dead before the measurement window. bench_train_step defaults byte-unchanged; 0.1.0 configs still validate. Session-id relocation ADJUDICATED FAITHFUL (plan named a function that doesn't exist in bench_train_step; no-stale-resume intent held via script_sha → new northstar session). Stub tests assert the CALL CONTRACT (ordering + exact hint values + eval-precedes-enable spy). Optional observations: northstar top docstring omits the attention dimension (build_probe comment has it); whole-file script_sha property untested (pre-existing, shared). Pre-campaign carry-forwards: real-model smoke FIRST; a "does not fit" verdict can now originate at the prewarm stage (correct binary-search semantics); never compare new northstar numbers to 0.1.0's (measured quantity changed BY DESIGN).)
- T13 Step 1 review: first opus attempt DIED on the session limit (resets 7pm CEST) mid-read; dispatch prompt PARKED at .superpowers/sdd/task-13-step1-review-dispatch.md per the handoff pattern; RE-DISPATCHED 19:09 right after the reset. GPU held idle during the review (serialization) — campaign starts on its verdict: real-model flash smoke → Step 2 single-op scaling → Step 3/3b train-step → Step 4 northstar.
- Task 13 Step 1 IMPLEMENTED (commit 012fadb, review IN FLIGHT opus review-mlx, package review-1c10ed3..012fadb.diff): 9 RED-first tests, default 543, ruff+mypy clean. Implementer deviations/notes: (1) session-id sensitivity applied in northstar where _recipe_session_id actually lives (bench_train_step mints fresh ids per invocation — no resume concept) + the real bench_train_step analog mirrored instead; (2) worker flash tests STUB enable (tiny fixture head_dim 16 outside gate) — first real flash run = first e2e exercise ⇒ CAMPAIGN OPENS WITH A SMALL REAL-MODEL SMOKE; (3) northstar ours arm now = flash+fused-CE ≠ 0.1.0's kernel-loss-only ours — old max-context numbers NOT comparable (note in STOP table; session-id change already prevents silent resume).
- Task 13 Step 1 DISPATCHED (2026-07-10, sonnet, BASE 1c10ed3): attention_impl threading (worker enable-after-cast-before-LoRA with seq/batch hints; --attention flag + session-id sensitivity; northstar per-ARM forcing in build_probe). Report → task-13-step1-report.md.
- Task 12: complete (commit 15fd9ee, review ✅ Approved opus review-mlx re-dispatch — COMPLIANT + Approved. LoRA path traced through installed mlx_lm 0.31.3 (apply_to_modules descends the wrapper; update_modules swaps the dict-child; attribute-lookup forward runs the adapter; lora_b>0 assertion falsifiable — closed-over-original ⇒ grad exactly 0 ⇒ test fails). Mask contract verified vs installed create_attention_mask (training N>1 ⇒ "causal"; N==1 ⇒ None; arrays only via return_array/window — refused). __call__ diffed line-by-line vs llama AND qwen3 stock (exact; _has_qk_norm branches). Pre-warm: one untraced fwd warms ALL 3 caches (api computes all rates on kernel branch); test pops all 3 keys byte-identical to production and proves re-population + unchanged cache size across compile(grad) trace. ADJUDICATIONS: unconditional head_dim gate = LITERAL brief compliance (not a deviation); dropout gate = dead-but-honest future-proofing (strictly conservative); 4→6 files both brief-implied. CARRY-FORWARDS: (1) drift lane must pin create_attention_mask's "causal"/None returns — controller fixing now; (2) batch_size pre-warm edge is loud-not-silent but T13 docs must say "pass batch_size = training batch" prominently (cache keyed on exact b); (3) T13 training-parity sweep should include a qwen3 arm (backward currently covered transitively). 56 default + 16 metal wrapper cases green, gates reproduce.)
- T11 Low-finding FIX (controller, 2026-07-10, commit 4a79805, tree clean): bench_attention_op warmup now runs one unmeasured value_and_grad (D-kernel JIT outside windows), wall_s = median of WALL_REPS=3 (+ walls_s field, smoke asserts median relation). EN ROUTE FOUND + FIXED a real measurement race: freeing warm buffers before the active_before snapshot races the allocator's deferred release → NEGATIVE marginal peak (measured −13 MB then −9.8 MB at N=256); fix = hold warm outputs alive through the windows + mx.synchronize() before each clear_cache/snapshot; smoke stable 5×. Repo CLAUDE.md gotcha 15 added. Gates: 15/15 --run-benchmark, 14+1skip default, ruff, mypy --strict.
- T12 review RE-DISPATCHED (2026-07-10): first opus reviewer died on the 600s stream watchdog ~4 reads in (no side effects); fresh review-mlx dispatched with the identical prompt + ignore-note for the uncommitted T11-fix files.
- Task 12 IMPLEMENTED (commit 15fd9ee, review IN FLIGHT opus review-mlx, package review-fdaf145..15fd9ee.diff): wrapper + refusals + hints-based pre-calibration (all 3 caches via one untraced call), loss-curve differential worst 4.768e-7→pin 2e-6, wrapper↔stock parity 9.537e-7→2e-6, LoRA-into-wrapper nonzero lora_b grads, subprocess gc child (+_attention_wrapper_gc_child.py), CODE_SHA_DEPS six-file completion (test_bench_resume.py RED pattern). 6 files vs brief's 4 (child helper + dep-test extension — reviewer adjudicates). Default 534, metal lane 1170, ruff+mypy clean. Implementer concerns: 2e-6 pins ~2-4× margin (cross-silicon widen-with-evidence signal); head_dim gate unconditional incl. reference; dropout refusal simulated (installed ModelArgs lack the field).
- Task 12 DISPATCHED (2026-07-10, opus implementer, BASE fdaf145, brief task-12-brief.md): enable_flash_attention wrapper. Controller ambiguity RESOLUTION in dispatch: pre-calibration needs runtime shapes (rate caches keyed by b/hq/n-bucket) → enable gains OPTIONAL kw-only hints (seq_len=None, batch_size=1); hints given ⇒ warm fwd+dq+dkv caches at enable so compiled train() traces warm; omitted ⇒ lazy-calibration for eager callers, compiled-train contract documented. Report → task-12-report.md.
- Task 11: complete (commit fdaf145, review ✅ Approved opus review-mlx — Spec PASS, both interpretive calls ACCEPTED (nargs="+" both-arms; fwd_peak recorded-not-ratioed per spec §8). ONE Low finding recorded for the pre-T13 controller fix (NOT dispatched): D-kernel JIT lands inside the measured fwd+bwd wall window (warmup is fwd-only; dq/dkv kernels JIT via calibration, launch_bwd_D does not) + docstring overclaims "JIT outside windows" + wall_s single-shot — peaks UNAFFECTED (JIT allocates no device buffers; the O(N) proof is clean). FIX before T13 runs the campaign: one unmeasured vag() in warmup + median-of-reps wall + docstring line. FIVE T13 campaign honesty carry-forwards: (1) peak fields are MARGINAL (active_before subtracted) — label as marginal/incremental, never absolute; consider adding active_before_gb/total_peak_gb for parity; (2) stock arm = math_attention O(N²) BOTH passes ≠ real mlx baseline (sdpa fwd is already O(N)) — never present stock fwd_peak as "what MLX does today"; (3) stock will OOM ≈N≥8192 (34 GB > cap) → WorkerCrashed artifacts EXPECTED, stock ratios likely only 2048→4096; (4) identity attention_impl carries arm label, variant/split unset (code_sha covers kernel provenance); (5) grid main returns 0 even on condition errors — orchestration reads artifact status, not $?.)
- Task 11 IMPLEMENTED (commit fdaf145, review IN FLIGHT opus review-mlx, package review-e412ef6..fdaf145.diff): scripts/bench_attention_op.py + tests (15/15; 14 pure + 1 gated N=256 smoke both arms). Mirrors bench_backward_ladder.py subprocess pattern (bench.worker kind-dispatch doesn't cover attention). Implementer interpretive calls to adjudicate: --impl nargs="+" defaulting both arms; fwd_peak_gb recorded-not-ratioed (spec §8 fwd+bwd wording); scoped regression run instead of full metal lane (additive change). Stale 0.1.0 task-11 report renamed (T10 precedent).
- STOP TABLE DECIDED (2026-07-10): Denis chose PROCEED (option 1) — backward ships at 3.1× fwd; deeper tuning deferred to 0.2.x pending T13's measured floor. T11 dispatched (sonnet, BASE e412ef6, brief task-11-brief.md).
- **TASK 9b COMPLETE (2026-07-10)** — e2e flagship through the PRODUCTION path (t9b_e2e_flagship.json, git e412ef6): public flash_attention impl="kernel" under value_and_grad — fwd 0.1828s (ladder-exact), fwd+bwd 0.7576s median, derived bwd 0.5748s (ladder predicted 0.587s — within 2%), grad marginal peak 1.002 GB (vs ~13 GB/layer stock SDPA bwd at 8k), NO refusal/kill on the first rate-driven mma split at saturation. FINAL LADDER: backward 18.4s-refused → 0.575s = 3.1× fwd wall (spec §4.4 target ~3× — MET); dq 35.2→2027.7 G (84% of the 2.4T CE anchor), dkv 82.9→1857.9 G (77%). All 4 steps done: Step 0 kill-model ground truth, Step 1 checkpoint, Step 2 rungs B1+B2+graduation (3 commits, 3 opus reviews, ZERO findings total), Step 3 individual commits + same-day ledger/skill updates. STOP TABLE presented to Denis (options: proceed to T11 [recommended] / keep tuning ~20-30% headroom / rescope). AWAITING DENIS. Caveats carried: rates same-session (±12% cross-session soft); compiled-step packing = T13 empirical; n>16384 calibration canary memory (North-Star sweep watch item).
- Task 9b GRADUATION COMPLETE (commit e412ef6, review ✅ Approved opus review-mlx — Spec PASS + Quality Approved, ZERO findings; 561 metal tests reproduced locally. _select_from_measured extraction behavior-preserving (28 fwd table tests green); d96-slab concern STRUCTURALLY closed (tables emit numeric slab only at d128; default slab 32 divides all dims); rate closures probe-their-own-kernel verified (dkv probe threads variant + zero-seeded partials); SAFETY_FACTOR identical to fwd; caches independent, zero dangling refs to the removed shared rate (grepped); api construction-time capture outside _core, vjp host-sync-free; block_align=32 derived inside launch_bwd_dkv, not bypassed; compile e2e at n24/d64 GENUINELY routes mma (provisional table) — compile proof covers the production variant class; refusal propagates on both paths; budget constants byte-identical. Concerns adjudicated non-blocking (off-bucket provisional rates = fwd rung-3 precedent, canary backstops; mma-everywhere small-shape routing parity-proven incl. bit-identical dQ split at NON-aligned boundaries n257). Carry-forwards → whole-branch: (a) flagship e2e = FIRST rate-driven mma split at saturation (controller runs next); (b) n>16384 calibration canary allocates n-key probe working set (ramp caps 8192, canary does not) — watch calibration-time memory if the North-Star sweep pushes past 16384, now on BOTH paths.)
- Task 9b GRADUATION IMPLEMENTED (commit e412ef6, review IN FLIGHT opus review-mlx, package review-a64b561..e412ef6.diff): select_bwd_tiles (two measured tables DQ/DKV_MEASURED artifact-cited, fwd bucket logic literally reused via _select_from_measured), calibrated_bwd_dq_rate + calibrated_bwd_dkv_rate (probe-own-kernel, variant/d_slab keys, independent caches; SHARED calibrated_bwd_rate + _BWD_RATE_CACHE REMOVED — single caller, no alias), api vjp construction-time tables+rates closure-captured, budget-comment amendment folded. RED-first all components; default 508, extended metal 400, untouched metal 176, ruff+mypy clean. Implementer concerns: off-bucket provisional rates honest-caveat (fwd rung-3 precedent); impl=kernel now routes MMA at ALL small shapes (existing e2e/compile tests confirmed matching oracle through the new path). NEXT after review: controller flagship e2e (t9b_e2e_flagship.py staged) + STOP table.
- Task 9b GRADUATION rung DISPATCHED (2026-07-10, opus implementer, BASE a64b561): backward dispatch table (select_bwd_tiles mirroring fwd occupancy-window rule; measured (128,[8192,16384))→mma/slab128 BOTH kernels non-provisional, sweep artifacts cited), per-kernel calibrated rates (probe-what-you-rate per kernel, variant/d_slab cache keys — kills the disproven shared-rate design), api vjp wiring (construction-time table+rates, closure-captured, vjp stays host-sync-free), Step-0 budget-comment amendment folded in. Report → task-9b-graduation-report.md. After its review: controller flagship end-to-end re-run + STOP table to Denis.
- Task 9b rung B2 SWEEP (controller, 2026-07-10, AC confirmed after a battery-block pause, artifacts rungB2_dkv_mma_slab{16,32,64,128}.json + rungB2_dkv_mma_full.json + rungB2_dkv_mma_sweep.py): **dK/dV MMA slab ladder at flagship canary: 16→513.41 / 32→903.23 / 64→1444.36 / 128→1857.94 G — monotonic larger-slab-wins, FOURTH kernel curve (dual accumulator ~256 fp32/lane still wins; skill entry updated). Winner slab128: REAL full flagship chained dK/dV pass 0.3829s median, causal-true 1435.77 G — 22.4× the scalar 82.89 G. xvar vs scalar 7.81e-3 = expected bf16-ULP cross-variant accumulation-order class (each variant's own chained≡single bit-identity is the contract and both hold). BACKWARD LADDER COMPLETE: D 0.0007 + dQ 0.2036 + dkv 0.3829 ≈ 0.587s vs fwd 0.1831s = 3.2× fwd wall (spec §4.4 target ~3×; was 100× at Step 1, 31× total improvement). Step 2 projection CLEARS the floor class.** NEXT: graduation rung (api vjp wiring + backward dispatch-table entries + per-kernel rate split + Step-0 budget-comment amendment), then STOP table to Denis, then T11.
- Task 9b rung B2 COMPLETE (commit a64b561, review ✅ Approved opus review-mlx — Spec PASS + Quality Approved, ZERO findings. Key-major MSL re-derived fragment-by-fragment (K-pointer bakes key via fm / Q-pointer bakes query via fn → Sᵀ(key,query) with NO transposes; Pᵀ column-L mapping verified element-wise; dV GEMM runs while C_s still holds Pᵀ, dSᵀ overwrite ordering verified; dSᵀ scale-folding ≡ scalar sds). CHAIN LIVE-PROBED at n=257: 5 ranges all 32-aligned, chained==single BIT-identical dK and dV, oracle 3.8e-6/5.7e-6; seed-first genuine (C_dv/C_dk loaded from partials before any block MMA); no-allowed-query key blocks write seed through (q_start=max(q_lo,key_base) no-op loop). plan_dkv_dispatches default byte-unchanged proven by pure test. Pins reproduced live (fp32 7.6e-6 < 2e-5; bf16 6.25e-2 = 1 ULP < 1e-1); GQA kill gap 3.88 reproduced; d96 slab-divisibility guard correct ({16,32} only). Concern-2 (256 fp32/lane dual accumulator) = rate carry-forward, slab128/d128 verified numerically clean; concern-3 flip probe valid (inverts the diagonal — the correctness gate; block-skip needs no own perturbation). Carry-forwards → graduation: per-kernel rate split; block_align=32 threaded on dkv path only (dQ disjoint, own alignment); whole-branch: re-verify shared-rate under mma cost model when both rungs graduate. 344 bwd + 179 fwd/api metal green, ruff+mypy clean.)
- Task 9b rung B2 IMPLEMENTED (commit a64b561, review IN FLIGHT opus review-mlx, package review-9096451..a64b561.diff): key-major MMA dK/dV landed per dispatch design — Sᵀ=K·Qᵀ/dPᵀ=V·dOᵀ fragments, L/D column-indexed, dV+=Pᵀ·dO BEFORE in-place dSᵀ overwrite, seed-first chained contract + plan_dkv_dispatches(block_align=) (default 1 byte-unchanged; mma→32), chained≡single array_equal + chained-vs-oracle forced-multi-range both green. Pins measured-first: fp32 dV 9.06e-6→2e-5; bf16 dV 6.25e-2 (1 ULP)→1e-1, separate _TOL constants. 344 bwd metal (+90), fwd/api/composition 210 green, full metal lane 1117, default 487, ruff+mypy clean. Implementer concerns: dual-accumulator ~256 fp32/lane at slab128 (sweep hypothesis: smaller slab may win); flip probe narrower-but-teethed (same as B1, diverges ≫1e-2). Controller sweep script READY: rungB2_dkv_mma_sweep.py (runs after review approval).
- Task 9b rung B2 DISPATCHED (2026-07-10, opus implementer, BASE 9096451): dK/dV MMA — one simdgroup per 32-KEY block per (b,kvh), KEY-major orientation (Sᵀ=K·Qᵀ, Pᵀ, dPᵀ=V·dOᵀ, dSᵀ; dV+=Pᵀ·dO, dK+=dSᵀ·Q — no fragment transposes; L/D column-indexed), register-resident D-slabbed fp32 accumulators, CHAINED contract preserved: seed-from-dk_in/dv_in FIRST + ascending query blocks + plan_dkv_dispatches gains block-alignment param (default 1 scalar-unchanged, mma passes 32) so splits land on block boundaries ⇒ chained≡single bit-identity survives at block granularity (mid-block split would merge partials inside one hw MMA). NOT wired into api/dispatch/calibration. Report → task-9b-rungB2-report.md.
- Task 9b rung B1 SWEEP (controller, 2026-07-10, artifacts rungB1_dq_mma_slab{16,32,64,128}.json + rungB1_dq_mma_full.json + rungB1_dq_mma_sweep.py): **dQ MMA d_slab ladder at flagship canary: 16→422.46 G / 32→744.18 / 64→1143.63 / 128→2027.67 G MAC/s — monotonic larger-slab-wins AGAIN (fwd rung-2b pattern reproduced; ~192 fp32/lane full-block slab128 wins 1.77× over slab64 — spill heuristic non-predictive at saturation, THIRD data point, skill updated same-day). Winner slab128: REAL full flagship dQ pass 0.2036s median (launcher-split at measured rate), causal-true 2025.13 G — 57.6× the scalar 35.2 G; xvariant diff vs scalar 0.00e+00 at n256 across ALL slabs.** Backward pole now dK/dV alone: projected bwd ≈ 0.20 (dQ) + 6.63 (dkv scalar) + 0.001 (D) ≈ 6.8s ≈ 37× fwd (was 100×). NEXT: rung B2 (dK/dV MMA, chained-partials contract preserved at 32-row block granularity), then graduation rung (dispatch entry + api wiring + per-kernel rate split), then STOP table.
- Task 9b Step 2 rung B1 COMPLETE (commit 9096451, review ✅ Approved opus review-mlx — Spec PASS + Quality Approved, ZERO findings. MSL re-derived fragment-by-fragment vs the oracle (S=QKᵀ byte-identical to fwd half; P from saved L per-row — correctly NO shuffle/rowmax; dP=dO·Vᵀ orientation verified; scale folded into dS ≡ oracle's dQ=scale·(dS@K); Kt slab load mirrors fwd Vt). Edges probed independently: n∉32ℤ tail keys masked by REAL kk not clamped pointer (n=33/100 → 8e-7/2e-6); slab recompute deterministic, parity identical across 16/32/64/96/128; mma≡scalar BIT-identical at n=130/d128; split-vs-oracle 9e-7 d96/n200. flip probe diverges 61–590 vs 1e-2 threshold (concern-4 narrowing = non-issue). Concern-2 (slab128 ≈192 fp32/lane full-block peak) = legitimate throughput hypothesis, correctly deferred; per-ct interleave (~72-tile peak) documented as the lever. 254 bwd + 167 fwd metal green, gating verified, ruff+mypy clean. Carry-forwards: rate from SWEEP only (ceiling flat 384 across dims AND slabs — non-discriminating, do NOT pick slab from it); graduation rung = dispatch entry + api vjp wiring + per-kernel rate split; expect smaller slab may win vs fwd's 128; MINOR for final review: no `kk<row` drop-diagonal flip perturbation anywhere (scalar+mma+fwd uniformly) — cross-cutting test-hardening item.)
- Task 9b Step 2 rung B1 DISPATCHED (2026-07-10 ~09:20, opus implementer): dQ MMA variant — one simdgroup per 32-row query block, S/dP MMAs + dS·K GEMM-B, register-resident D-slabbed fp32 dQ accumulator (template d_slab, default 32, 16/32/64/128 buildable), fwd-mma causal predication, scalar default untouched, NOT wired into api/calibration (graduation later). Small shapes only; controller owns saturation sweep after review. REVIEW BASE = 2ccbee0. Report → .superpowers/sdd/task-9b-rungB1-report.md. Rung B2 (dK/dV MMA, chained partials preserved) queues behind B1's review.
- Task 9b Step 1 DONE (controller, 2026-07-10 ~09:15, artifact _artifacts/attention_bwd_rungs/step1_bwd_timing.json + step1_bwd_timing.py): **flagship (b1/32q/8kv/N8192/D128/bf16/causal) — fwd (mma/slab128, table-selected) 0.1831s median / 1501 G causal-true; D-kernel 0.7ms; dQ canary 35.2 G; dK/dV canary 82.9 G. Projected causal-true full backward = 18.35s ≈ 100× the fwd wall (spec §4.4 reference: ~3×). At the production calibrated rate (35.84 G safety-halved) BOTH backward splits REFUSE flagship (dq 23s / dkv 31s projected vs 2.0s budget) — the review's 'correct-but-refusing' functional gap is CONFIRMED, Step 2 tuning rungs REQUIRED (the no-tuning branch is dead).** Review carry-forward (1) answered: **shared-rate condition FAILS measured** — throughput_dq 35.2 G < 0.5·throughput_dkv 41.45 G (real gap 2.35×, not the predicted 4/3; hypothesis: dkv's GQA group loop reuses k/v registers across 4 q-heads, dq streams k/v per row with no reuse) ⇒ dQ dispatches sized by the shared rate run ~0.51s REAL (intended ~0.25s; 2× margin consumed) ⇒ **rate must be SPLIT per-kernel — fold into the rung/graduation work (calibration gets rebuilt there anyway), MUST land before T12/T13.** Carry-forward (2) answered: compiled ceilings (inverted-signal reading) dq 384/384/1024 (register-resident d64/96, SPILLED d128), dkv 1024 flat (spilled at every head_dim — 4×D fp32 arrays/thread) yet dkv still 2.35× faster ⇒ both scalar bodies memory-bound, spill not the limiter, MMA is the lever (T6 ladder anchor: 31.6→1462.7 G). NEXT: Step 2 rungs — dQ MMA first (reuses fwd rung-2 machinery; longer pole), then dK/dV MMA; STOP table to Denis after rungs with measured ladder.
- Task 9b Step 0 DONE (controller, 2026-07-10 ~09:0x, AC power, pre-flight clean): buffer-packing ground-truth probe — **13/13 conditions SURVIVED**, including single-eval K=32 (~8.6s wall, the same class rung 0 saw KILLED), eval-split + async_eval K=32 variants (~7.9–9.2s), and real 0.5s/1.0s single dispatches. Combined with rung 0's same-machine kills this GROUND-TRUTHS the mechanism: **the interactivity kill is SYSTEM-STATE-DEPENDENT (display/UI contention), not a fixed per-buffer threshold** — kill-active day boundary (2.2, 8.7)s; permissive day ≥9.2s survives. Consequences: (a) MAX_DISPATCH_SECONDS 0.5 / MAX_TOTAL_SECONDS 2.0 STAY (worst-observed-day governs; a permissive-day probe can never license raising them); (b) eval-split vs async_eval flushing INDISCRIMINABLE on a permissive day (no kills → no differential) — rung-0 packing evidence stands; (c) compiled-step packing risk at MMA-class rates remains governed by the ~2s worst-day class. Artifacts: _artifacts/attention_bwd_rungs/buffer_packing_probe/*.json (13) + probe_buffer_packing.py (resumable, re-run on a kill-active day to catch the tight boundary). user-metal-kernels workflow-and-gotchas kill entry REWRITTEN same-day (state-dependence is now the durable fact). launch.py budget comments: amend at the T9b commit (note the 2026-07-10 permissive-day probe as why 0.5/2.0 stand un-raised).
- Task 9: complete (commit 2ccbee0, review ✅ Approved opus review-mlx re-dispatch 2026-07-10 — Spec PASS + Quality Approved, ZERO findings. dK/dV MSL re-derived term-for-term vs the oracle (GQA group loop, owner indexing, causal swap all exact); the chained≡single bit-identity argument CONFIRMED to hold (range boundaries at query-row boundaries ⇒ the per-i q-head inner loop is never split; fp32 store/reload lossless); plumbing clean (fresh buffers, no eval in chain loop, cast-once); planner refusal arithmetic reproduced by hand; ALL pins reproduced live (fp32 dK 5.007e-6/dV ~1.1e-5 vs 2.5e-5; bf16 dV 6.25e-2 = exactly 1 ULP at [8,16) verified, pin 1e-1 < 2-ULP 0.125; GQA kill gap 3.8805 = 155× margin; e2e compile 2.123e-6 vs 1e-5); vjp traced host-sync-free (bwd_rate closure-captured float; planner pure int math; compile(grad) test empirically confirms); forward byte-untouched (200 fwd tests green); budget constants byte-identical. ADJUDICATION of the shared-backward-rate call: SOUND for T9 but the "heavier ⇒ conservative" rationale is imprecise — the real safety condition is throughput_dq ≥ 0.5·throughput_dkv, held by the 2× SAFETY_FACTOR (intensity gap only 4/3), currently UNMEASURED. **BINDING carry-forwards → T9b:** (1) Step 1 must measure BOTH backward kernels' achieved MAC/s and confirm the shared-rate condition (dQ cannot self-detect an under-budget — it uses the same shared rate); re-check/split the rate if a future MMA dK/dV diverges from dQ. (2) d128 register-pressure characterization (kreg/vreg/dk/dv = 4×128 fp32/thread) — perf-only, parity already passes. Carry-forward → final whole-branch review: T9 correctness kernel + T9b production timing are ONE unit — a flagship LaunchBudgetError refusal is honest but is a functional gap the branch must close.)
- Task 9 review RE-DISPATCHED (2026-07-10 ~08:40, resumed session): the handoff's in-flight reviewer was interrupted ~8 tool-calls in (no verdict); fresh opus review-mlx dispatched with the RECOVERED original dispatch prompt (saved .superpowers/sdd/task-9-review-dispatch.md) + an explicit controller-authorized-deviations block (the 4 inherited design points, per the T7 false-positive lesson). T9b prep done in parallel (no GPU): Step 0 probe WRITTEN at _artifacts/attention_bwd_rungs/probe_buffer_packing.py (subprocess-per-condition, resumable; K-sweep single-eval 4..32 + eval_every8 + async_every{4,8} controls + single-dispatch 0.5s/1.0s length conditions; child measures real per-dispatch wall at the safe 0.25s class and scales), runs AFTER the review frees the GPU.
- Task 1: complete (2026-07-09, controller; commits 30f0585 pending-docs + 3e8ada6 bump. **GO — the full 0.1.0 kernel contract REPRODUCES on mlx 0.32.0:** metal lane 536 passed / 2 skipped, cov gate met, all parity pins unchanged, compiled-ceiling pins EXACT (dense RT4 448 / RT2 640, quant 384 via test_devtools in-lane). pyproject bounded >=0.32.0,<0.33; VERIFIED_MLX_VERSIONS += 0.32.0 (RED-first: allowlist test + installed-mlx-is-verified invariant test). **0.32.0 findings:** wheel split mlx/mlx-metal; core .pyi stubs DROPPED from the wheel → vendored the 0.31.2 stubs under typings/ (mypy_path, ignore_errors override for the stub files, ruff extend-exclude) so mypy --strict keeps checking mx.* — delete when upstream restores stubs; re-check per bump. user-metal-kernels api-contract.md updated same-day. AC + memory pre-flight clean. Repo CLAUDE.md pins section updated locally.)
- Task 10: complete (commit ae92bd5, review ✅ Approved sonnet — RED independently VERIFIED against the base commit in a worktree (11 failed with exactly the claimed mechanisms); the 5 paths byte-checked vs the tree; _code_sha basename-collision traced safe (sequential digest, no name-keying); reserved-kwargs mechanism endorsed as the only meaningful "hijack guard" reading; the GLOBAL-invalidation question ADJUDICATED: intended behavior — one flat CODE_SHA_DEPS is the established model, spec §8 frames the risk one-directionally as under-coverage, false-positive cost is recompute minutes not correctness; optional per-kind scoping = a low-priority efficiency backlog item ONLY if T13's campaigns measure real annoyance. Implementer archival call (stale 0.1.0-era task-10 brief/report renamed, not overwritten) was right. 44 bench tests, metal lane 862. wrapper.py's sixth entry lands at T12 per the amended plan. Old artifacts globally invalidate on next resume BY DESIGN — T13 recomputes everything fresh anyway.)
- Task 9: STALLED on the session limit (2026-07-10 ~00:xx, resets 1am) — the opus implementer died AFTER its design read, touching nothing. ITS DESIGN DECISIONS (validated, inherit on re-dispatch): (1) owner = one thread per (batch, kv_head, key) — the v1 analogue of T8's per-query-row owner; (2) per-pair MAC = 4·D (s-dot + dp-dot + dk-acc + dv-acc), per-query-row cost 4·D·n·b·hq; (3) chained≡single bit-identity mechanism: initialize accumulators FROM the incoming partial FIRST, then accumulate this dispatch's range with query-row OUTER / q-head INNER / ascending — a range split then reproduces the single-dispatch sequential order exactly; (4) calibrated_bwd_rate reuses the fwd ramp via an additive macs_per_row parameter. T10 dispatched in parallel (sonnet lane, zero file overlap) as the limit probe.
- Task 8: complete (commit 09ebdc7, review ✅ Approved opus — dQ MSL re-derived BYTE-EXACT vs the api.py oracle (scale folding, saved-L usage, D consumption, causal skip all traced); flip-probe failure mode confirmed the intended causal-off-by-one detector; shared-helper refactor verified byte-identical for the forward (167 fwd tests green, same messages/arithmetic); 3D-per-row MAC model honest-conservative; pins reproduced exactly (fp32 2.465e-6→5e-6 ~2×; bf16 exactly 1 ULP 1.5625e-2→3e-2 <2 ULP); both disclosed calls adjudicated CORRECT (api deferral authorized by controller dispatch + MLX-correct; 32/8@64 reading matches the fwd precedent). Low for final review: _validate_bwd_dq_shapes doesn't assert lse/d_arr fp32 (unreachable in practice, completeness nit). 95 bwd tests, metal lane 851. NEXT: T9 dK/dV split-partials + api vjp wiring.)
- Task 7: complete (commit 561fce3 + comment-fix commit pending; review sonnet: Spec PASS — MSL reduction re-derived by hand (lane striding exact for 64/96/128, grid arithmetic traced), pins reproduced exactly (fp32 7.63e-6→2e-5, bf16 3.81e-6→1e-5), can-fail probe discriminates, no-guard call independently MEASURED at 0.638 ms/dispatch flagship (~780× under budget). Quality Needs-work→FIXED: (1) reviewer's "fabricated brief quote" = FALSE POSITIVE adjudicated by controller — the quote is verbatim from the CONTROLLER DISPATCH (binding guidance the reviewer couldn't see); implementer's error was attribution ("the brief"), corrected in the report; (2) the committed "thousands of times under budget" comment overstated the measured ~780× → both source comments pinned to the measured figure. 26 tests, metal lane 782. NEXT: T8 dQ kernel.)
- **TASK 6 COMPLETE (2026-07-09):** rung 3 commit c0a315d (sonnet — dispatch.py table mirroring core's occupancy-window pattern, measured (128, [8192,16384)) → mma/slab128 non-provisional; TileShape d_slab+provisional fields; API wired through select_fwd_tile; fixed the latent calibrate-what-you-rate bug — measure() previously always probed scalar). **Consolidated 3-commit review (opus review-mlx): Spec PASS, Quality Approved, ZERO correctness/MLX hazards** — the register-resident softmax fully re-derived (fragment layout vs steel facts, shuffle butterflies, alpha rescale row-sync, GEMM-B contraction, D-slab recompute determinism, tail predication, GQA); canary block-granularity traced to a non-issue (~3% inside the 2× margin); all three rung-3 interpretive calls endorsed. 3 doc findings fixed same-session (controller doc pass, commit pending): stale D-slab rationale reframed to the measured truth (the 57%-regression trap), launch docstring's "threadgroup-resident K/V" corrected to register-level, devtools ceiling test gains the SHIPPED flagship (128, slab128) case. Carried to final review: mma calibration-shape rigor gap (honest docstring caveat, re-arm candidate); pre-existing mx.metal.is_available() namespace sweep. Forward ladder FINAL: 31.6 G → 86.75 → 932 → **1462.7 G (60% of CE class), full flagship fwd ~0.19-0.31s, peak 0.281 GB memory-flat.** Gates: metal lane 756 passed. NEXT: T7 (D-preprocess kernel).
- Task 6 rung 1 (2026-07-09): MMA restructure DONE commit 6df36d8 (opus subagent, re-dispatched after the session-limit stall; parity-exact, full lane 724 passed, d=128 spill inversion resolved — ceilings mma 576/384/384 vs scalar 384/384/1024; no scalar pin widened; RED honest). **MEASURED (controller, canary-basis, flagship shape): 86.75 G MAC/s — 2.7× over rung 0's 31.6 G scalar, ~25× short of the CE MMA class (2.4 T); full forward correctly NOT run (projected ~3.2s > the 2s per-eval class).** Artifact rung1_mma.json. Diagnosis (from the implementer's own recorded levers): per 32-key block, one small MMA then S-staging + scalar softmax + a 16 KB threadgroup O-accumulator round-trip with barriers — threadgroup O-traffic dominates. Rung 2 = the recorded levers: P@V as a second MMA (the proven backward GEMM-B machinery) + register-resident O tiles with D-slabbing + unroll audit. External anchor: metal-flash-attention reaches ~2.3 T-class fwd on this hardware, so the ambition is real, not aspirational.
- Task 6 rungs 2/2b (2026-07-09): rung 2 commit dff6f70 (opus — register-resident P@V MMA O-path, ALL threadgroup traffic/barriers eliminated, simd_shuffle row reductions, per-thread-element alpha rescale; parity identical, lane 727). **MEASURED at flagship (controller, canary-first + full-forward, artifacts rung2*_*.json): default slab32 = 931.7 G; d_slab sweep at SATURATION: 16→514, 32→932, 64→1361, 128→1462.7 G (parity 1.95e-3 identical across all).** Ladder: 31.6 → 86.75 → 932 → **1463 G = 60% of the CE MMA class**; full flagship forward median 0.305s→~0.19s, peak 0.281 GB (memory-flat ✓). TWO measured surprises recorded: (a) the tiny-N larger-slab ranking SURVIVED saturation (gotcha-5 verification done right — verified, not assumed); (b) **d_slab=128 (C_o=64 tiles = the predicted spill zone) does NOT collapse — the register-arithmetic spill heuristic over-predicts simdgroup-accumulator cost at saturation** (echoes 0012: pressure telltales ≠ rate predictors). Mechanism unknown → small research item to file (backlog, next admin pass); the dispatch-table entry is chosen from the MEASURED ranking, which is valid regardless. NEXT: rung 3 = graduate the winner into dispatch.py (measured 128@8192-bucket entry + provisional flags), wire the mma variant + its OWN calibration into the API path (auto currently still selects scalar!), then T6 CLOSES — forward at 1.46 T is defensible for 0.2.0 (the backward is the flagship bottleneck; deeper fwd tuning = 0.2.x item if T13's floor demands).
- Task 15: complete (controller main-thread 2026-07-09 under TDD, commit pending confirmation — the MMA subagent died on the API session limit (resets 7:20pm) so the limit window went to this parallel-lane task per the plan's P7 note. Device-proportional caps: `wired/soft = min(base_gb·GiB + 0.85/0.92·max(0, dev_max − 25 GiB), 0.85/0.92·dev_max)` — byte-identical at/below the reference BY CONSTRUCTION (M1 Max preservation pin vs the real 26,800,603,136 B → exactly 20/22 GiB), scaling + monotonicity + panic-proof tests for 16–512 GB classes; signature/kwargs untouched (zero consumer blast radius). RED→GREEN: scaling test failed at the fixed ceiling, 9/9 after. Gates: default lane 393, ruff, mypy. Backlog 0019 → DONE (Done table row; unblocks 0015 kit build). T6 rung 1 (MMA restructure) dispatch prompt preserved in this ledger's transcript context — RE-DISPATCH after the limit resets.)
- Task 6 rung 0 (controller, 2026-07-09): **v0 scalar baseline = 31.6 G MAC/s canary-derived at flagship shape (b1/32q/8kv/N8192/D128/bf16)** — artifact `_artifacts/attention_fwd_rungs/rung0_scalar_v0.json`. TWO measured findings en route, both fixed TDD (commit pending, launch.py + tests): (1) self-shaped ramp probes read 3.4× optimistic (106 G) vs the true DRAM-bound range-dispatch rate → calibration now ends with a CANARY (tail row-range against FULL n keys — the only production-shaped probe under causal; measure(rows, keys) seam refactor + _dispatch_range extraction). (2) **macOS kills per COMMAND BUFFER / cumulative eval GPU-time, not per dispatch** — two ImpactingInteractivity kills (projected-1.0s dispatch; then 35 honest ~0.25s dispatches in one ~8.7s eval) vs the CE kernel's never-killed ~2.2s evals → MAX_DISPATCH_SECONDS 0.5 + MAX_TOTAL_SECONDS 2.0, both evidence-pinned in tests; flagship v0 forward now refuses honestly (it can NEVER ship at that rate — MMA is the fix, which is T6's whole point). Skill updated same-day (per-buffer kill mechanism, threshold in (2.2, 8.7)s, packing inferred); plan T9b gains Step 0 (buffer-packing ground-truth probe) — the chained backward is the next exposed path. Gates: module 86, metal lane 652, default 390, ruff+mypy clean. NEXT: dispatch the MMA restructure (T6 rung 1), measure in the next quiet window. — MSL online-softmax recurrence re-derived line-by-line (alpha-order, l-update, L=m+log(l), fp32 accumulators all correct); causal predicate-before-rowmax + in-kernel GQA verified; split qoffs arithmetic exercised by a genuinely-split 9-dispatch bit-identical test; parity pins reproduced (fp32 9.54e-7→2e-6; bf16 1-ULP 7.81e-3→1.2e-2; L 1.43e-6→5e-6); wrong-mask CAN-FAIL flips genuinely. Deviations adjudicated: per-key predicate instead of loop-bound skip = ACCEPTABLE v0 staging (enables the flip test; T6 owns loop bounds); repurposed not-built-yet test = required, no refusal weakened. Fix round closed the review's High (fixed 128-probe was cache-optimistic vs DRAM-bound flagship → N-aware ramp calibration, rate from the largest safe stage only, budget-safe per stage) + Medium (global mx.random.seed side effect → local split keys, stream-untouched test). Review finding 3 (host-synced calibration in flash_attention's body vs future compiled train) DEFERRED BY DESIGN → binding inherited requirement added to plan T12 (pre-calibrate at enable + no-calibration-inside-trace test); re-review confirmed not worsened. **Ceiling-inversion mechanism → user-metal-kernels same-day (reviewer-ruled): dynamically-indexed per-thread arrays past the spill threshold INVERT the regpressure signal (D=128 ceiling 1024 vs D=64 384 — spilled arrays read as low pressure); numbers v0-ephemeral, mechanism durable.** v0 ceilings for T6 baseline: 64→384, 96→384, 128→1024. Metal lane 649 passed. Minors for final review: none new (fix re-review found only CE-inherited safe-direction nuances, no action).)
- Task 4: complete (commit d65ba25, review ✅ Approved opus — all 7 vjp equations re-derived + independently probed at GQA 8q/2kv N=13 causal (~1e-6/3e-5 vs oracle, incl. kv-head reduction shape + causal-boundary NaN-freedom); residual contract verbatim per T2 (o,_ discard; vjp reads outputs); tolerance pins reproduced BIT-EXACTLY (3.814697e-06 n=96 / 1.668930e-06 n=61, pin 2e-5 = honest 5.24x); refusal grid match=-pinned; VJP_CALLS adjudicated KEEP (mirrors shipped chunked.py precedent; counter only in the uncompiled regime, value-parity in compiled; value-discriminator provably unavailable for a correct vjp); check-order (dtype→head_dim→causal→metal→verified) endorsed as user-friendly. Implementer disclosures accepted: single import-error RED (not per-assertion), fabricated-tolerance near-miss caught pre-commit (committed values measured). Minors for final review: 4 unexercised _validate_shapes branches (batch/hkv-hv/N/D mismatch tests); add stay-uncompiled comment on the counter test; optional both-files VJP_CALLS privatization consistency pass. 21/21 tests, default lane 369.)
- Task 3: complete (commit 736892b, review ✅ Approved opus — oracle math re-derived AND matched against an independent NumPy reimplementation (O 1.4e-7, L 2.7e-7); causal diagonal-inclusion, fp32-upcast-before-matmul (bf16-safe L), GQA `//` at both ratios, FD-anchor independence + every-kv-head-grad-flow all verified; both implementer calls adjudicated (plain ValueError in the private oracle guard OK; FD re-pin 1e-3 over measured 3.258e-4 = the measure-first discipline working). Minors for final review: causal=False + bf16 cast-back branches untested (out of brief scope); the sdpa-forward pin is 4.4× over measured (cross-implementation margin, defensible). 9/9 tests, default lane 356.)
- Task 2: complete (2026-07-09, controller, TDD RED→GREEN — **VERDICT: GO.** All five proofs landed in tests/test_attention_composition.py (8 passed) + tests/_composition_gc_child.py: (1) sentinel-gradient proof that a hand vjp fires under mx.compile; (2) under mx.checkpoint AND the full compile(grad(checkpoint)) nesting; (3) full-stack: subprocess child ran 2 real compiled mlx_lm train() steps at gc=True with every attention output through a custom_function — COMPOSITION_OK (subprocess isolation per gotcha 13; the in-process llama patch site in test_worker_train_step is cross-referenced); (4) GQA convention PINNED: contiguous `q_head // group_size` at 4q/2kv and 8q/2kv (interleaved `% Hkv` explicitly discriminated); (5) moat: installed 0.32.0 SDPA fwd+bwd peak still O(N²) (>3×/doubling) — this test FAILING in the future = upstream shipped the fix, replan. **New verified MLX fact (recorded in the test docstring): mx.checkpoint reduces grad peak ONLY under mx.compile — uncompiled measured WORSE (824 vs 736 MB); compiled 448 vs 632 MB. All 0.2.0 checkpoint-memory assertions must be made under compile.** Deviations from plan text: recompute-count assertion replaced by sentinel-value + compiled-peak-delta (trace counters unobservable under compile — review-mlx amendment applied); child exercises train() via worker's _run_train_steps (the real path) with default_loss, full-train() composition at the wrapper level lands at T12. mlx 0.32.0 API strictness fix en route: mx.allclose no longer accepts scalar args. Gates: module 8/8, ruff, mypy, default lane 347. Backlog 0017 → DONE (verdict recorded, Done table row); 0018 → in-progress. NEXT: T3 (pure-MLX oracles, subagent).)

# ═══ 0.1.0 (shipped 2026-07-08) ═══
# mlx-train-perf 0.1.0 — subagent-driven execution ledger

Plan: /Users/ionden/Documents/Work/mac/docs/superpowers/plans/2026-07-03-mlx-train-perf-0.1.0-plan.md
Scratchpad (briefs/reports): /private/tmp/claude-501/-Users-ionden-Documents-Work-mac-mlx-train-perf/efeb293e-2171-4111-af23-34a0cd2b0209/scratchpad/

- Task 1: complete (measure gate, main session — no repo commits; artifacts mlx-train-perf-spike/results/gate_*.json + custfn_tuple_outputs.json; STOP resolved 2026-07-03: PASSED, Denis pulled 0008 into 0.1.0 → plan Task 16b; decision logged in backlog 0002)
- Task 2: complete (commit bbf9fc4 — amended once more for the reviewer's ⚠️ fetch-depth fix; review ✅ Approved; public repo github.com/IonDen/mlx-train-perf, auto-delete on, dev/v0.1.0 pushed + checked out, codegraph init done; CI run 1 green both jobs, METAL PROBE: OK → coverage gate goes to the metal-tests CI job in Task 7; logged in 0002)
  - Minor carried forward → Task 5: extract/test cap arithmetic (clamped_caps) and refactor conftest to consume install_guardrails
- Task 3: complete (commit babea9f, review ✅ Approved — reviewer independently re-verified stub visibility + noqa legitimacy; mypy fix: scoped numpy override + python_version 3.12 + mlx stubs restored)
- Task 4: complete (commit d6ac214, review ✅ Approved — oracle math re-derived by reviewer; one disclosed import-hoist for lint)
- Task 5: complete (commit b88fdbe, review ✅ Approved — clamp arithmetic hand-checked both cases; conftest refactor onto install_guardrails, T2 carried-forward finding CLOSED)
- Task 6: complete (commit fc558e9 after comment-fix amend; review ✅ Approved) (opus; gradient math verified from first principles; tolerance widenings ADJUDICATED CORRECT — brief's bf16/quant gates were wrong-source transplants; root cause: bf16 logit rounding pre-upcast, confirmed by chunk-width-invariance + same-dtype control 4.77e-7). One Important comment misattribution being fixed via amend (quant error is common-mode; driver is d-scale). Pins: fp32 2e-6/2e-8, bf16 4e-3/8e-5, quant 9e-3/4e-5, control 2e-6, engine 1e-8.
  - Watch item for final review: tight fp32 pins (2e-6/2e-8, ~2.1-2.7x margin) could flake cross-device if CI's GPU reassociates differently than M1 Max — widen with documented note ONLY if CI actually flakes.
  - Downstream note: Task 11's quant d_hidden placeholder (2e-2) should pin near T6's measured 1.53e-5-class; README should state chunked-vs-exact bf16 error (~1.6e-3) honestly vs kernel's 1.9e-6.
- Task 7: complete (commits d9cb8ea + 3b9d189, review ✅ Approved opus — all 5 adjudications pass: launcher protocol faithful, sentinel templating provably lossless vs both spikes, pins honest 4e-6/5e-6 (measured 1.907e-6 both dtypes — kernel's fp32-in-register accumulation means NO bf16 logit rounding), pyproject E501 per-file-ignore justified, coverage gate fires in metal lane at 97.53%). CI metal-tests job live.
  - Minor folded into T8: grid-scope comment on the fp32 pin (spike's broader grid saw 4.8e-6 — widen toward 1e-5 ceiling on new shapes, not a regression)
- Task 8: complete (commit 685b10d after review-fix amend; review ✅ Approved — probe arithmetic hand-verified, timing/cache semantics confirmed; Important fixed: probe inputs zeros→seeded random per reviewer's exact prescription; zeros-vs-random comparison recorded: random HIGHER 110-175 G vs 40 G zeros, ~60% spread = wall-clock noise dominates at tiny probe scale, no compression bias observed; noise direction is guard-conservative post-SAFETY_FACTOR)
  - Watch item: if any probe sample ever reads >2x the true rate (would make the guard permissive), switch probe to median-of-3 — revisit at T16b/T17
  - Pre-existing mypy tests/-only findings (test_guards.py, test_chunked.py) — outside CI's mypy src/ scope; for the final whole-branch review
- Task 9: complete (commit 2974ef8, review ✅ Approved — verbatim match, all six dispatch points hand-verified)
  - Minor for T11: select_variant has no n<=0 guard — confirm loss.py's input validation runs BEFORE resolve_impl reaches it
- Task 10: COMPLETE 2026-07-04 (STOP resolved: Denis ruled optimize-the-variant; optimization succeeded in 2 rungs; hoist graduated 09db141 + ramped calibration 622d922, both reviews ✅ Approved; committed bench re-run: quant 2029.2 G vs dense 2437.3 G = 1.20x ≤ 1.5x ✓, memory delta 0.83 GB ≈ head size ✓ — both release thresholds MET, all conditions ok. 0009 acceptance 3/4 ticked, mlx-lm checkpoint verification rides with T12/T17. 0011 fixed+verified (sweep ratios 0.98-1.00); 0012 filed P3.) (orig: commit 50abff9 after review-fix amend: fp32-head ablation committed as the tight 1e-5 dequant-correctness anchor, measured exact 0.0; review ✅ Approved opus — B-load-only contract statically replayed, bf16-oracle root cause confirmed against quantized.h, skill update compliant; 94 metal tests green). STOP RULING FROM DENIS PENDING (rate threshold missed 5.8x — recommendation: dequantize-once contingency). Controller bench fix being restored from stash as its own commit. (orig commit 92b3cec; parity pinned 8e-3 with oracle-side bf16 rounding root-caused via fp32-scales ablation hitting exact 0.0 — OUR kernel is more accurate than mx.dequantize; regpressure ceiling 384 vs dense 448 — cliff warning FIRED; skill updated same-day). Code review pending.
  - THRESHOLD BENCH VOID (2026-07-03 evening): machine power-degraded — battery 7% charging, powermode 2; spike's own v2e control measured 766 G vs morning's 2423.7 G with 6x rep instability (2.1/12.9/6.7 s). Dense 1104 G + quant 134 G readings DISCARDED. Re-run when power stable (battery >=60%, then a rep-stability probe within ~5% before the full bench). T10 STOP deferred until then.
  - Bench script bug FIXED (controller, 2026-07-04): refusals recorded as `refused` artifacts; artifacts path was fine (my glob was wrong).
  - 2026-07-04 re-run on stable power (battery 78%, spike control 2422.2 G at 0.05% rep spread — machine recovered; degraded artifacts trashed, v2e provenance artifact re-measured): dense 2439.5 G / dequant-once 2448.6 G / quant REFUSED at calibrated 211 G post-safety (~421 G raw ≈ 5.8x slower — rate threshold MISSED). Logged in 0009. T10 STOP presented to Denis. Code review in flight (opus).
  - 2026-07-04: **T10 STOP RESOLVED — Denis ruled OPTIMIZE THE VARIANT** (over dequant-once contingency and ship-slow). Quant-kernel optimization rungs proceed under 0009 (rung 1: RT=2, unmeasured, compiled ceiling 512; rung 2 candidate: hoist per-group scale/bias loads out of mtp_dq4). T11 NOT dispatched — gated on the ladder outcome (bar: ≥ dense/1.5 ≈ 1630 G same-session). Ruling propagated to 0009 (in-progress), 0002 log, plan T10-step-10 note, roadmap index.
  - 2026-07-04 rung 1 + 1b: quant RT=2 parity-proven (fp32 ablation exact 0.0) and measured 778.8 G at n=8192/tile 8192 (guard accepts; 1.85x over RT=4-quant, still 3.12x vs dense RT=4 = MISS). Decomposition via new dense-RT=2 control (1587.3 G): 1.53x RT=2-structure x 2.04x dequant-overhead. Full log + artifacts in 0009 / _artifacts/quant_opt_rungs/. Rung 2 (scale/bias register hoist, both RTs) in flight.
- Task 10b: complete (commit 09db141, review ✅ Approved opus — all four hoist deltas byte-verified vs the measured reference, load contract statically replayed incl. reload nesting + clamped columns, fma reorder bit-inert, parity pins held at both RTs; metal 106 passed, coverage 98.29%)
  - Minor for final review (pre-existing, out of 10b scope): source.py module docstring names mlx-train-perf-spike/kernel_v2*.py paths — a shipped-docstring local-path mention; clean up before release per the public-docs rule.
- Task 11: complete (commits 3a2ac15 + fix 4129b5b, review ✅ Approved opus — dispatch truth table verified branch-by-branch, VJP-engagement question settled empirically (dropped kernel vjp = loud ValueError at grad trace, not silent), both implementer interpretive calls endorsed (n<=0 guard in resolve_impl; explicit impl="kernel" still gates); quantized d_hidden pinned at measured 1.526e-5 -> 4e-5; fix round closed all 4 findings (quant value-parity pins 9e-3/1e-3/6e-2 measured-first, explicit-kernel gating pinned, integral-targets validation, version monkeypatch aligned), fix re-review ✅ zero new issues; 152 metal-lane tests, loss.py 100% cov)
- Task 12: complete (commits 537d689 pin + 5cf8381 adapter + f5c5aa5 smoke + fix e06976a; review ✅ Approved opus — mask math verified EXACT vs installed default_loss incl. prompt-offset/fully-masked-row/ntoks==0 crash-parity; live-weight-staleness catch praised; fix round closed the Important (mypy override narrowed to implicit_reexport=true, typo-probe-proven checkable) + 3 minors (ntoks==0 test, pin widened >=0.31.3,<0.32, quantized-through-adapter parity 0.0 pinned 1e-5); fix re-review ✅ zero new issues. Adapter 100% cov.)
  - Informational for final review: T12's middle commit had narrative-only RED (tests+impl same commit; substance independently verified); adapter's per-step target-range validation raises where stock computes-then-masks (stricter-than-stock, deliberate); smoke test (gated) needs pre-downloaded mlx-community/Qwen3-8B-4bit — controller runs it at T17.
- Task 13: complete (commits 9ccdea3 + fixes b483e5c + 1b4e6eb; review opus Spec ✅ / initial quality Needs-work → both fix rounds verified — untied-embedding drop fixed with independent re-pin, whole-model quant pricing per controller ruling (reproduces real 8B int4 checkpoint size), naive coefficient REFRAMED as empirical production-shape fit after the re-review disproved the causal buffer story with the n=2048 sibling artifact (~2×; no linear fit exists; anchor shape degenerate 2D/n=1) — hedged language + conservative-direction test (>= measured at n=2048) shipped, anomaly filed as 0013 (P3); tracker-phrase leaks purged from shipped docstrings (controller-verified by grep + diff read, 16 plan tests green). plan/ 100% cov, 186 metal-lane total.)
  - (superseded fix-round note kept for history: base commit 9ccdea3, review Spec ✅ / quality Needs-work scoped to quantized-weights branch). CONTROLLER RULINGS recorded: (1) quantization pricing scope = ALL quantized params at quant rate (body+head), superseding the brief's underspecified head-only formula — body-at-bf16 would phantom-refuse the flagship 4-bit recipe; (2) naive loss coefficient becomes a Calibration constant reconciled to the measured gate artifact (brief's analytic x2 under-models 1.9x).
  - DOWNSTREAM WARNING (from review, do not inherit): the plan/brief claim "LoRA q,v-proj = mlx-lm's default target set, verified in Task 12" is FALSE — installed mlx_lm 0.31.3 linear_to_lora_layers zero-config default targets EVERY Linear/Embedding per block (~5x more LoRA params; ~84 MB scale on 8B r=8, <0.5% of budget). Planner models the conventional q,v LoRA-paper formula with an honest docstring. T17 calibration and any docs copy must NOT say "matches mlx-lm defaults".
  - Known model limits (review, accepted for 0.1.0): full-FT rank=0 optimizer/grad state unmodeled (docstring note lands in fix round); is_estimate is blanket rather than per-term provisional flags.
- Task 14: complete (commits 34e60ec + fixes 560e3c8 + 1345d70; review Spec ✅ / quality initially Needs-work, TWO fix rounds — round 1 closed the 5 directed findings incl. the Critical code_sha identity ruling (hatch-vcs version proven 5 commits stale under editable install) and measured slower/faster ratio direction; round 2 closed the re-reviewer's 3 REPRODUCED residual gaps (stale-artifact-served-as-ok on silent worker failure → unlink-before-spawn; params could hijack ANY identity field incl. code_sha → self-maintaining overlap guard in run_identity; core/naive.py added to CODE_SHA_DEPS) — closure verified by controller running the reviewer's exact reproducers. 33 bench tests, 219 metal-lane total, 99.67% cov. Incidental: pre-existing tiny-shape g_mac_per_s rounding flake fixed during x5 stability verification.)
  - Forward notes for T15 (from T14 report): loss_layer condition params schema is T14's design choice — T15 CLI must consume it as-is; report() ratios are keyed "slower/faster" with value >= 1 (measured direction, settled in fix round 1).
- (superseded in-flight note: base commit 34e60ec, review Spec ✅ / quality Needs-work.) CONTROLLER RULING: run_identity gains code_sha over the declared measured-path dep list (supersedes the brief's "package version" wording — reviewer reproduced hatch-vcs version 5 commits stale under editable install; uv sync does not refresh it; resume could serve stale results). Also: ratio direction becomes measured slower/faster (was alphabetical, sign convention could flip between impl pairs).
  - PRE-RELEASE SWEEP ITEM (consolidated, for final review / T18): shipped src/ docstrings citing local-only spike paths are SYSTEMIC — core/kernel/source.py (flagged at 10b review), plan/estimate.py, plan/calibration.py, bench/artifacts.py. One sweep, reword to self-contained provenance before the release PR.
- Task 15: complete (commits 0b2d55b + fix 1194d35; review Spec ⚠️→✅ / quality Needs-work→fixed — one High reproduced (--quant-bits silently flipped a config's gs=32 to gs=64 → over-optimistic fit verdicts; fixed: preserve config-derived quant_group, default 64 only when metadata absent; RED-first regression test) + exit-policy line added to bench --help; refused→exit-1 policy ENDORSED by review (harness retries refused on resume), follow-up --allow-refused filed as 0014 (P3). Controller-verified via the new tests + help grep. 31 CLI tests, 191 default lane.)
- Task 16: complete (commits 1d63433 + fix 1a59023; review Spec ✅ / Approved — fd-capture verified genuinely fd-level, 448/640 ceilings independently reproduced, semantics docstring "stronger than the minimum bar"; fix round parameterized the kernel contract (needed by T16b's backward-kernel probing) + repaired the demonstrably-failing quantized docstring example; the parameterized path reproduced the quant RT=4 ceiling at 384 = exact match to the T10/rung-2 history. 8 devtools tests, 258 metal-lane total.)
  - CI-verification RESOLVED 2026-07-04: pushed bbf9fc4..1a59023 (28 commits), CI run 28716261878 GREEN all three jobs (checks / metal-probe / metal-tests) — the 448/640 ceiling pins REPRODUCE on the GitHub macos-15 runner's Metal toolchain; ceilings are cross-environment-stable on mlx 0.31.2. Everything T10-graduation→T16 is CI-verified.
- Task 16b step 3: complete (commit d467b77, review ✅ Approved opus — math re-derived correct, atomics contract verbatim per step-1 ground truth, simdgroup mapping race-free; TOLERANCE HONESTY EMPIRICALLY PROVEN: bf16 worst 0.125 = exactly one ULP at the max-magnitude entry |d_w|=19.0 (rel 0.66%), pins 2e-4 fp32 / 0.25 bf16; rerun independence proven by nonzero fp32 spread 8.6e-6; kernel is MORE accurate than the bf16 oracle. Ceiling 1024 (v0, no pressure). Fix round in flight: per-chunk astype before concatenate (bit-identical, halves production transient 4.98->2.49 GB = staged baseline level) + bench_backward_ladder.py for step 4.)
  - Step-4 baselines (gate artifacts, to re-measure same-session): staged vjp 6.21 s trainable / 4.83 s frozen; staged trainable marginal peak 2.466 GB. Ladder bar: beat staged on wall at <= its memory, or levers exhausted -> STOP table to Denis.
  - Step-4 ladder run 1 (2026-07-04 late): ramp rate-accounting bug (tile-MACs over full-chain wall -> ~1187x-low fiction, 0 G refusals). FIXED b7aed9d (chain MACs: rate=n*v*d/elapsed) + script_sha added to identity + buggy artifacts Trashed.
  - Step-4 ladder run 2 (2026-07-05, post-fix): rates now HONEST. staged_vjp_frozen 5.29 s / 8.50 GB, staged_vjp_trainable 6.55 s / 7.91 GB (re-ran fresh, ~1.5% off run 1). kernel_dhidden_v0 REFUSED with a REAL rate: "projected 1.09 s/dispatch at 4 G MAC/s" -> v0 backward runs ~8 G MAC/s raw (300x slower than fwd's 2428 G), BELOW the 10 G FLOOR_RATE the launch envelope assumes -> guard correctly refuses (probe_tile_for's floor-tile still over budget). kernel_dw_v0 + combined refuse the same way. LEGITIMATE result: untuned v0 cannot dispatch at production; tiling mandatory.
  - **BENCH-CORRECTNESS FINDING (must fix before STOP table):** staged_vjp_* run `impl="chunked"` = the chunked FALLBACK forward + chunked vjp, NOT the shipping STAGED mechanism (kernel forward + chunked vjp) the gate measured (2.47 GB). Explains the 8.5/7.9 GB vs gate 2.47 GB divergence (different forward path). Relabel to chunked_fallback_*, ADD true staged_kernel_* (impl="kernel" fwd + chunked vjp) = the real baseline the fused backward competes against.
  - **HONEST RUNG ARITHMETIC (fwd 5.10 TMAC @ 2428 G / 2.10 s; frozen bwd = 2x fwd MACs, trainable = 3x):** v0 1274 s (matches the refusal projection - validates it). v1 register-block (~800 G class) 12.75 s frozen / 19.1 s trainable = LOSES to staged by 2.5x -> NOT WORTH BUILDING (guaranteed wall-loss). MMA (~2400 G class) 4.25 s frozen / 6.37 s trainable: frozen MMA MARGINALLY beats staged (4.25 vs ~4.8 s ~= 12%), trainable MMA LOSES (6.37 vs 6.2 s). => Only frozen-MMA can win on WALL, barely; MEMORY (no logit materialization) is the robust win at any rung. This is the "one lever left, marginal payoff" case.
  - **RUNG STRATEGY (decided):** SKIP v1 register-block (arithmetic already proves it loses). Build ONE MMA-logit-regen d_hidden FROZEN rung (flagship path; the only rung whose measurement could change the wall verdict; reuses the forward's proven 4x4 MMA logit machinery + adds P-formation + a first-cut output accumulation) -> MEASURED anchor (workspace distrusts projections per 0011/0012). Then STOP (step 5) with the table: staged_kernel baseline + v0-refused + MMA-measured + trainable-arithmetic + memory numbers + honest recommendation (likely: staged-default for 0.1.0, fused backward as a 0.2.0 memory play — Denis decides kernel-vs-staged).
  - Run 2 COMPLETE 2026-07-05: all 3 kernel_v0 conditions refused (honest 4 G/s guard rate); staged_vjp_* measured but mislabeled (chunked fallback). Tree clean.
- Task 16b step 4 (MMA d_hidden frozen rung): CODE DONE commit 0fff13e (opus impl). Fused two-GEMM: GEMM-A = forward's 4x4 MMA logit-regen reused BYTE-IDENTICAL (_GEMM_A_LOGIT_REGEN sliced by stable anchors, asserted in test); in-place P-formation; GEMM-B = per-lane DISJOINT device RMW of d_hidden ((fm,fn) bijectively partition the 8x8 tile per steel/gemm mma.h get_coord), NO atomics, chained across vocab sub-tiles like the forward chains lse. Parity-exact vs BOTH chunked_backward oracle AND v0 (fp32 8.0e-7/pin 2e-6, bf16 1.95e-3/pin 5e-3 — v0's class). COMPILED CEILING 448 (RT4)/640 (RT2) = the forward's OWN ceiling, NO DROP (P-form+GEMM-B added no register pressure — better than the arithmetic assumed). Tiny-shape sanity 50 G MAC/s (occupancy-starved, NOT a production number). 424 metal tests, launch.py 100% cov. GEMM-B device-traffic-vs-threadgroup-staging left as a measured question for a later rung (honestly recorded). Review ✅ APPROVED opus: GEMM-B PROVABLY race-free (bit-set disjointness {4,2,1}⊔{3,0}, cross-checked byte-for-byte vs Apple steel/gemm/mma.h get_coord, + 4-run bit-identical determinism), GEMM-A byte-identity guaranteed-by-construction (Python slice), P-formation static-replayed correct, parity reproduced to the exact float, ceiling-no-drop real. One cosmetic Low (construction-guaranteed test `in` assertions can't fail — content assertions have the teeth; carry to final review, NO fix). SOUND TO MEASURE.
  - Bench STOP conditions: commit 13a75c1 (review-by-controller: honesty-guard RED-confirmed staged_kernel resolves to kernel not chunked; 9 conditions measure at --tiny incl. kernel_dhidden_mma 87.6 G = 38x v0). Renamed staged_vjp_*→chunked_fallback_*; added staged_kernel_* (impl=kernel), kernel_dhidden_mma (MMA backward alone), chunked_dhidden_alone (chunked backward alone) — the direct MMA-vs-chunked wall+memory comparison.
  - STEP-5 STOP TABLE MEASURED (2026-07-05, production, stable — spreads 0.6-2.1%, internally consistent): staged_kernel_frozen 4.81s/4.50GB (incumbent, matches gate), trainable 6.06s/4.97GB; chunked_dhidden_alone 2.71s/4.50GB (the backward to beat); **kernel_dhidden_mma REFUSED ~31.5s @ 162 G MAC/s = 11x SLOWER than chunked bwd — GEMM-B first-cut device-RMW traffic dominates (the flagged measured-question, answered: dominates hard)**; v0 refused ~1275s. Full table + verdict in 0008.
  - **STEP-5 RULING: Denis chose A — staged-default for 0.1.0, fused backward → 0.2.0.** Step 6 = NO code change (loss.py already staged-default: chunked_backward in every vjp; kernel-backward launchers NOT exported = internal groundwork exercised by parity tests). Full suite GREEN 429 passed post-ruling. 0008 → P2/proposed/de-gated (0.2.0: threadgroup-staged GEMM-B is the one measured lever); groundwork (v0/MMA-regen/d_w-atomics/ground-truth/regprobe/ladder script) all committed + parity-proven + reviewed, stays in tree. 0.1.0 story = fully-fused FORWARD + proven chunked vjp.
- **TASK 16b COMPLETE 2026-07-05** (steps 1-6 done; step-5 STOP ruled A). Backward-kernel workstream paused at parity-proven groundwork per the ruling; NEXT PLAN TASK = T17 (acceptance benches; STOP at bar-pinning + North-Star sweep needs Denis's explicit go).
- Task 16b step 2: complete (commit c0656a5, review ✅ Approved opus ZERO defects — math re-derived from first principles, static replay proved the -1 target term fires exactly once (backward uses exact-tcols, no clamp duplicates — cleaner than forward), simd_sum uniformity + race-freedom verified, parity worsts reproduced to the bit; fp32 pinned 2e-6 (measured 8.05e-7), bf16 5e-3 (measured 1.95e-3 = one ULP at the output cast). v0 ceiling 1024 (no pressure, expected). Devtools output-side generalization adjudicated correct+needed.)
  - Forward notes for steps 3/6 (review): tgt residual is DEAD in the whole backward family (d_hidden and d_w need only lse+targets) — step-6 seam should unpack only lse, or keep bundle-uniformity deliberately; backward needs its OWN calibrated rate at wiring (v0 achieved rate is far below the forward's MMA rate — never pass the forward's calibrated_rate).
- Task 16b step 1: complete (commit 6c2a0e8; atomic_outputs=True PROVEN correct — bit-exact vs split-K oracle, up to 5,600 contending threadgroups; native atomic<float>/atomic_fetch_add_explicit contract folded into the skill same-day; cost tie ~4.5% at representative shape BUT atomics save the ~512 MB split-16 partials buffer → step-3 verdict: ATOMICS, with the run-to-run d_w non-determinism caveat for the step-5 STOP. Verdict + artifacts logged in 0008. Step 2 (d_hidden-only backward v0, frozen-head first) next.)
- Task 17 BUILD: commit 142c168 (502 tests, worker train_step kind + 3 bench scripts + fit_calibration + WiredCapRegressionError; calibration_data.json untouched = still placeholders). Steps 4/5/6 (real calibration write, bar-pinning STOP, final commit) = controller.
  - **CORE DEFECT SURFACED BY T17 (compile-compat) — FIX IN FLIGHT:** loss.py `_validate_inputs` does `mx.min/max(targets).item()` (target-range host-sync). mlx_lm.tuner.trainer.train() ALWAYS wraps its step in mx.compile, which FORBIDS evaling a traced array → our loss CANNOT run in the standard compiled trainer, for ANY impl (reproduced: all 3 impls break). NOT caught in T11/T12 (they used manual uncompiled steps). CONTROLLER-VERIFIED the full surface + fix: (a) the target-range check is the ONLY wall — calibrated_rate is compile-SAFE (it evals its own independent probe arrays, cold OR warm; my "second wall" hypothesis was WRONG, tested); (b) gating ONLY the target-range sync makes all 3 impls compile-safe, numerically identical (7.0568). Fix = validate_targets flag (adapter=False; direct API=True/safe-default) + revert bench_train_step's uncompiled "ours" workaround (was making the comparison UNFAIR: ours uncompiled vs stock compiled). T17 train-step benchmark BLOCKED on this fix (can't run fair until both arms compile).
  - **COMPILE FIX DONE + VERIFIED END-TO-END (controller, main thread — the fix subagent died on a session limit before starting, so I drove it under TDD):** loss.py validate_targets flag gating the target-range host-sync; adapter passes False (documented); worker.py refactored — deleted _run_manual_steps, both arms now run through the real compiled train() (_run_train_steps), unused nn/apply_grad_checkpoint imports removed. test_loss_compile.py locks it (RED-first: all impls break under compile today → GREEN with the flag; kernel metal-gated; safety both directions; numerical inertness == 0.0). PROVEN at 3 levels: unit, kernel-under-compile (metal), and a REAL mlx_lm.train() run ("Starting training..., iters: 2", ours loss 12.887→12.126). 511 passed, ruff+mypy clean.
  - **TWO MEASUREMENT-PHASE FINDINGS (resolve before the kernel-vs-stock benchmark, NOT blockers for the fix):**
    1. **bf16-loading: RESOLVED.** The 4-bit MLX models compute in fp16 at runtime (quantization stored fp16 scales, overriding config torch_dtype=bf16), so the kernel path (fp32/bf16 only) refuses them via auto. FIX: `model.set_dtype(mx.bfloat16)` after `mlx_lm.load` casts everything to bf16 and the trunk hidden becomes bf16 (VERIFIED: Llama-1B fp16→bf16 after set_dtype) — the kernel arm then works. Wire a bf16-cast option into the bench's train_step worker for the kernel arm (small). Slightly changes numerics vs the fp16 original (bf16 vs fp16 scales) — fine for a throughput bench, note it. mlx_lm.load has no dtype arg (model_config dict exists but set_dtype is simpler).
    2. **qwen2 arch unsupported:** adapter split_model supports llama+qwen3 only (0.1.0 scope). Smoke had picked Qwen2.5-0.5B (qwen2) → fixed to Llama-3.2-1B-4bit (llama). Qwen2.5 is hugely popular — worth a backlog item for 0.2.0 arch coverage.
    - Also added: bench_train_step --impl flag (default auto; kernel needs bf16); smoke forces chunked + Llama.
  - COMPILE FIX REVIEW ✅ Approved (sonnet, thorough — traced target-consumption safety in all 3 impls: kernel equality-only/never-offset, chunked clipped, naive explicit-opt-in-only; confirmed mlx_lm padding = token 0 in-range; worker refactor loses nothing vs train() source, even removes a hidden _clear_cache asymmetry; all gates reproduced 511 passed). One Low nit (2 stale Qwen2.5-0.5B docstrings) FIXED + amended → commit 55d405c. Compile fix DONE.
  - qwen2 arch gap → file a 0.2.0 backlog item (adapter supports llama+qwen3; Qwen2.5 popular).
- **T17 MEASUREMENT PHASE — step 1 loss-layer DONE (PASS):** kernel loss-layer peak 0.0006 GB vs naive 2.318 GB at n=8192 (~3900x less memory) for 1.636x forward wall (<=1.7 bar). Full table in 0002. Kernel memory-flat across n (never materializes logits). Artifacts _artifacts/bench_loss_layer/.
- **T17 MEASUREMENT PHASE (remaining):** step 1 loss-layer done. Then: wire bf16-cast into the train-step worker's kernel arm → run train-step bench (Denis-go not needed) → NORTH-STAR sweep (Denis's explicit go) → calibration update → bar-pinning STOP (Denis pins the bar).
- **T17 step 2a — bf16-cast wiring DONE (2026-07-05, session resume, controller under TDD):** `worker.run_train_step` reads a `compute_dtype` param (default None); when set it `model.set_dtype(_resolve_dtype(...))`s the loaded model's floating params right after load, before freeze/LoRA (int4 weights stay int4 via set_dtype's floating-only predicate — verified mlx 0.31.2). `bench_train_step.py` gains `--compute-dtype {bfloat16,float32,float16}` threaded uniformly into BOTH arms' params (smoke stays None/chunked). RED→GREEN both sides: `test_run_train_step_casts_model_to_compute_dtype` + `_without_compute_dtype_leaves_model_dtype_unchanged` (worker), `test_build_conditions_threads_compute_dtype_into_both_arms` + `_defaults_to_none` (bench). Gates: ruff ✓, mypy --strict src ✓, default lane 318 passed/199 skipped. **FRAMING DECISION (controller, present at STOP):** cast BOTH arms to bf16 (not ours-only) — isolates the loss layer in the tok/s + loss-curve comparison; fp16≈bf16 trunk speed on M-series; flagship OOM story unaffected (logits 2 bytes either way). **Committed fca1323** (4 files, 89+/5-). Cached models confirmed (Qwen3-8B-4bit flagship + Llama-3.2-1B/3B-4bit).
- **T17 step 2b — AC restored, de-risk PASSED, production bench RUNNING (2026-07-07):** Denis plugged into AC (97%, attached; mem 86% free). Kernel-path de-risk on the REAL Qwen3-8B-4bit (untied) at seq 256, `--impl kernel --compute-dtype bfloat16`: ours_status=ok, stock_status=ok, loss-curve worst diff 0.0025 (bf16-class), confirming the bf16-cast lets the kernel arm run end-to-end through the compiled trainer on the production model. (Seq-256 numbers occupancy-starved: ours 0.45x tps + MORE memory than stock — expected, stock's logit buffer is tiny at n=256; the win is at large context. Not a production number.) Artifacts `_artifacts/bench_train_step_derisk/`. Production bench launched (bg id boqh4us8x): Qwen3-8B-4bit seq {1024,2048,8192}, batch 1, r=8 all-layers, 20 steps, both arms bf16, `--impl kernel`, ~30-60 min. First condition landed: **seq1024 ours-kernel status=ok, 109.3 tok/s, total_peak 16.54 GB, wired cap HELD at 20.0 GB** (in-loop reassert works). ours marginal 12.25 GB at seq1024 (grad_checkpoint=False → trunk activations dominate) → **seq 8192 may OOM BOTH arms in this run.**
- **T17 step 3 PREP — North-Star bf16-cast bug FIXED (2026-07-07, controller under TDD, committed cbdcbf5):** `northstar_context_sweep.py` `build_probe`/`make_probe`/`sweep_arm`/`_recipe_session_id`/`main`/parser now thread a `compute_dtype` (default bfloat16, `--compute-dtype` flag) into both arms' probe params — WITHOUT it the ours-arm loads fp16, auto refuses the kernel, worker crashes, and ours' max context would falsely read 0. 4 RED→GREEN tests + session-id sensitivity; 24 northstar tests pass, ruff clean. **OPEN DECISION for Denis at the North-Star go:** grad_checkpoint. Current probe default False → trunk activations dominate → ours' loss-layer advantage is a small fraction of total, so ours' max-context edge over stock ≈ the logit-buffer size only. Realistic long-context QLoRA uses grad_checkpoint=True (recompute activations) → loss-layer memory becomes binding → ours' context advantage is larger and the claim is meaningful. Recommend North-Star run grad_checkpoint=True (needs a probe param + flag, small TDD add) — raise with Denis before the run.
- **T17 step 2 — PRODUCTION TRAIN-STEP BENCH COMPLETE (2026-07-07, bg boqh4us8x, AC/stable, spreads reasonable):** Qwen3-8B-4bit, both arms bf16, grad_checkpoint=False, 20 steps. Artifacts `_artifacts/bench_train_step/`.
  - seq1024: ours 109.3 tok/s vs stock 132.5 → **ours 0.825x** (−17.5%); marg peak ours 12.25 / stock 11.95 GB; loss-worst-diff 0.00242.
  - seq2048: ours 66.0 tok/s vs stock 70.7 → **ours 0.934x** (−6.6%); marg peak ours 27.33 / stock 27.31 GB (total_peak 31.6 GB — near 32 GB RAM ceiling); loss-worst-diff 0.00146.
  - seq8192: **BOTH arms OOM** (WorkerCrashed) — no grad_checkpoint → 27 GB activations at seq2048 already; 8192 needs ~100+ GB.
  - Wired cap HELD at 20 GB in every ok condition (in-loop reassert works).
  - **KEY FINDING (present to Denis; NOT a methodology error):** at grad_checkpoint=False the trunk activations DOMINATE the step-level peak (27 GB at seq2048), so ours' loss-layer memory advantage is INVISIBLE end-to-end (ours ≈ stock memory, even +0.02 GB) and BOTH OOM at 8192. The ~3900x loss-layer win (step 1, isolated) only becomes the BINDING end-to-end constraint under grad_checkpoint=True (activations recomputed → loss layer dominates → ours fits much longer context). So: (a) the seq1024/2048 tok/s + loss-parity data is valid (ours 7–17% slower/step, converging toward parity as context grows); (b) the MEMORY/CONTEXT win requires grad_checkpoint=True — North-Star MUST use it, and a train-step re-run at grad_checkpoint=True would show the step-level memory advantage + a more favorable (realistic) tok/s. STOP: presenting to Denis for North-Star go + grad_checkpoint decision + scope.
- **T17 STOP RESOLVED (2026-07-08): Denis chose Option A — North-Star @ gc=True + re-run fits-both train-step @ gc=True** (explicit go for the 1–2h North-Star given). grad_checkpoint=True confirmed as the realistic long-context setup + the only regime the memory/context win shows.
  - **grad_checkpoint threading DONE under TDD (controller):** `bench_train_step.py` gains `--grad-checkpoint` (store_true, default False) threaded into both arms' params; `northstar_context_sweep.py` threads `grad_checkpoint` (default TRUE, `--grad-checkpoint/--no-grad-checkpoint`) through build_probe/make_probe/sweep_arm/_recipe_session_id/main. 6 RED→GREEN tests, 48 passed both modules, ruff clean. Commit in flight (grad_checkpoint threading).
  - **SEQUENCE:** (1) validating train-step re-run @ gc=True seq{1024,2048} RUNNING (bg bfnfqvlpp, separate out-dir _artifacts/bench_train_step_gradckpt to not clobber the gc=False artifacts, ~15–25 min) — de-risks the "memory win now visible" hypothesis (expect ours marg_peak < stock) + gives realistic tok/s before committing to the long run. (2) THEN North-Star @ gc=True (1–2h). (3) THEN calibration + bar-pinning STOP.
- **T17 — VALIDATING gc=True RE-RUN DONE (2026-07-08, bg bfnfqvlpp): HYPOTHESIS CONFIRMED.** Qwen3-8B-4bit, both arms bf16, gc=True, seq{1024,2048}, artifacts `_artifacts/bench_train_step_gradckpt/`.
  - Memory dropped ~9× vs gc=False (seq2048 ours 27.33→3.03 GB). ours' advantage now VISIBLE + grows with context: seq1024 ours 2.15 / stock 2.12 GB (≈equal), **seq2048 ours 3.03 / stock 3.75 GB → stock +0.72 GB** (≈ its (2048,151936) logit buffer). Delta scales with N → ours fits longer context.
  - Realistic tok/s: ours 0.923x (seq1024) / 0.877x (seq2048) = ~8–12% slower; loss parity 0.0024/0.0015. (The gc=False seq2048 tok/s were paging-degraded at 31.6 GB; these gc=True 3 GB numbers are the clean ones.)
  - Rough ceiling extrapolation (2-pt): both arms' marginal ~linear in N; ours ~+0.86 GB/1024tok, stock adds ~+0.35 GB/1024tok logit term → est. stock ceiling ~20–24k, ours ~28–32k → **~1.3–1.5x context advantage** (to be measured, not a huge multiple — trunk activations, even recomputed one-layer, grow with N and dominate; the loss-layer delta is the minority). Kernel guard checked SAFE at 32k (per-vocab-tile dispatch ~0.46s < 1s budget; ceiling is memory-OOM not guard).
- **T17 step 3 — NORTH-STAR LAUNCHED @ gc=True (2026-07-08, bg, AC 88% charging, mem 86% free):** `northstar_context_sweep.py` default recipe (Qwen3-8B-4bit, batch 1, r=8 all-layers, seed 0, compute_dtype bf16, grad_checkpoint True, start 1024, granularity 256, both arms). Resumable (stable recipe session_id, per-probe artifacts _artifacts/northstar_context_sweep/). ETA ~30–90 min (est.; ceilings look ~20–32k). Monitoring probe artifacts for progress; will flag if it probes past ~32k (would mean the estimate was low + slow high-context probes).
- **T17 step 3 — NORTH-STAR COMPLETE (2026-07-08, bg bv6trs1k4): DISPROVEN PREDICTION — ours does NOT fit more context; ours ≈ stock (TIED, ~8500 tok).** session 435c2ef. Results: **ours max_fit 8448 / min_fail 8704; stock max_fit 8704 / min_fail 8960** — stock fits 256 tok (one granularity step, ~3%) MORE than ours = within noise = essentially TIED. Full marginal-peak trail (identical for both arms at the boundary):
  - seq1024 ours 2.15 / stock 2.12 (+0.04) | seq2048 ours 3.03 / stock 3.75 (**−0.72**, ours less) | seq4096 ours 6.96 / stock 7.48 (−0.52) | **seq8192 ours 21.39 / stock 21.39 (+0.00, IDENTICAL)** | seq8448 ours 21.94 (fits) | seq8704 ours OOM / stock 22.18 (fits).
  - **ROOT CAUSE (high confidence, from the data):** memory is SUPER-LINEAR in N — increments 1024→2048→4096→8192 are +0.88 / +3.93 / +14.43 GB (~4.5× per doubling = O(N²)). A shared O(N²) trunk term — almost certainly attention scores (N,N); mlx-lm's training path is NOT flash-O(N) here — DOMINATES the peak at seq≥8192 (21 GB), and it peaks during the TRUNK BACKWARD, AFTER the loss-layer transient is freed. So stock's (N,V) logit buffer (2.49 GB at 8192) and ours' flat loss do NOT affect the PEAK → identical (21.39 both) → same ceiling. The 0.72 GB loss-layer advantage is real ONLY at small context (seq 2048), where the loss layer IS the peak — exactly where you don't need it. At large context (where you'd want it) attention O(N²) swamps it.
  - **IMPLICATIONS (major, for Denis):** the core "trains longer context / survives where stock OOMs" claim is NOT supported end-to-end for Qwen3-8B (V=151936) in the mlx-lm training path — ours ≈ stock max context. STILL VALID + measured: (a) loss-layer memory in ISOLATION ~3900× less (step 1); (b) throughput ours ~8–12% slower/step; (c) loss parity bf16-class; (d) at a GIVEN context, ours saves the logit buffer (0.5–0.7 GB visible when trunk is small). The context win WOULD likely show for (i) much larger vocab (stock's N×V logit term grows, ours flat), or (ii) an O(N) flash-attention training path (removes the O(N²) trunk dominance) — both = re-arm candidates.
  - **PROCESS:** disproven-prediction rule engaged → STOP, presented to Denis before building further; will create a backlog item + review artifact + re-research + re-armed spike per his steer. Bar-pinning/docs must NOT assume the context win.
  - **ROOT CAUSE VERIFIED (2026-07-08, direct mlx test):** `mx.fast.scaled_dot_product_attention` (mlx 0.31.2, mlx-lm base.py:130 — Qwen3 uses it) has a FLASH O(N) forward but an **O(N²) BACKWARD** (materializes the (N,N) attention matrix). Measured single-SDPA peak: fwd 96/192/384 MB at N=2048/4096/8192 (O(N), 2× per doubling); fwd+bwd 924/3408/13057 MB (O(N²), ~3.8× per doubling) → **one layer's attention backward = 13 GB at N=8192.** THIS is the binding constraint for training context on MLX, shared by both arms, and it swamps any loss-layer saving. **Strategic consequence:** the Liger-style "fused CE extends context" value prop (which works in PyTorch because flash-attn there has an O(N) backward) does NOT transfer to MLX 0.31.2 — the attention backward, not the loss layer, is the context bottleneck. The fused-CE loss is still a correct, useful building block (isolation ~3900× mem, competitive throughput, exact parity, saves the logit buffer at a given context) but does NOT extend max trainable context for realistic models on current MLX. PRESENTED TO DENIS for the 0.1.0 release-story decision.
  - **DENIS'S STEER (2026-07-08):** research first, then decide — invoked `/deep-research` on memory-reduction techniques for MLX training on limited-memory Macs (weaving in the O(N²) attention-backward finding). Workflow launched (bg wy48wjm2z). 0.1.0 release-story decision (reframe / drop context claim / pursue attention backward) PAUSED pending the report. T17 bar-pinning / T18 / calibration all on hold.
  - **Disproven-prediction 3-part review admin DONE:** backlog item **0017** created (P1 research, roadmap index updated) + shareable review artifact `docs/superpowers/reviews/2026-07-08-mlx-train-perf-context-win-disproven-review.md` (part 1). Deep-research = part 2 (running). Re-arm = part 3 (pending research + Denis). NEXT: when research lands → save as a proper research paper under `docs/superpowers/research/`, integrate top levers into 0017 re-arm section, bring integrated picture (finding + research + options) to Denis for the 0.1.0 decision.
  - **DEEP-RESEARCH DONE (2026-07-08, wf 108 agents/26 sources; workflow's final synthesis returned STUB placeholders — real claims recovered from journal.jsonl and synthesized by hand).** Paper: `docs/superpowers/research/2026-07-08-mlx-train-perf-training-memory-reduction-research.md`. **KEY RESULT: the O(N²) attention-backward bottleneck is FIXABLE — FlashAttention's backward is a proven O(N)-memory algorithm** (recompute S/P tiles on-chip from saved O + (m,ℓ); ~2.5× fwd FLOPs, net-faster via bandwidth; 20× mem; FA2 in PyTorch). A Metal flash *forward* already runs on Apple Silicon (arXiv 2607.00501); the O(N) *backward* Metal kernel is the open gap = **highest-leverage lever** = same tiled-recompute idea as our CE kernel, applied to attention. Secondary: selective activation checkpointing (~3% vs ~50–100%) + activation/layer offload (near-free on unified memory). Our fused CE validated by Cut-Cross-Entropy (ICLR 2025, identical architecture) — buys batch/headroom at fixed context, not context. Optimizer-quant = full-FT lever, minor for QLoRA. 0017 re-arm section updated. **Reframes project: "fused loss" → "tiled-recompute memory layer for MLX training (loss + attention)."** NEXT: present integrated picture to Denis for the 0.1.0 framing + 0.2.0 roadmap decision.
  - **DENIS'S DECISIONS (2026-07-08):** (1) **Ship 0.1.0 with the honest reframe** — fused-CE never materializes (N,V) logits (à la Cut Cross-Entropy): saves loss-layer memory + buys batch/headroom AT A GIVEN CONTEXT, ~8–12% step cost, exact parity. DROP the "trains longer context / survives OOM" claim; note max context on MLX is attention-bound (point to roadmap). Flash-attn backward = 0.2.0 flagship. (2) **Attention-backward kernel: NOT YET** — 0017 stays proposed/P1, revisit after 0.1.0.
  - **RESUME PLAN (honest 0.1.0):** back to T17 step 4 (calibration) → step 5 (bar-pinning STOP: Denis pins the ~8–12% throughput bar) → step 6 commit → T18 (docs with the honest reframe + North-Star disclosure + roadmap pointer). **NEW COMPLICATION from the finding (resolve at bar-pinning STOP):** the planner's activation model is LINEAR in seq (estimate._activation_bytes) but real memory is O(N²) (attention backward) AND grad_checkpoint-dependent (~9× gc True/False) — both unmodeled. Options: (a) ship linear + honest caveats; (b) improve to a linear+quadratic activation model fitted from the North-Star gc=True probes (seq 1024/2048/4096/8192 marg 2.15/3.03/6.96/21.39 — spans the O(N²) transition) so the planner correctly predicts the ~8500 ceiling; (c) descope planner from 0.1.0. Present with numbers at the STOP.
  - **DUE-DILIGENCE on the attention-backward moat (2026-07-08, Denis asked):** (a) mlx-core SDPA backward is O(N²) for ALL configs (causal/none/bool-mask/head_dim 64+128, ~3.83×/doubling — not a fallback bug); (b) mlx#2955 flash-attn proposal was inference-only + CLOSED, no maintainer plan for a training backward; (c) `philipturner/metal-flash-attention` (standalone Swift/Metal) HAS an efficient backward (solves Apple FP32-atomics via 7-GEMM alt) but NO MLX integration. Moat = real but not impregnable; our value = first MLX-integrated/autograd-wired version, ideally a **C++ mlx `fast::` primitive**. Logged in 0017.
  - **C++/low-level question (Denis):** the hot path is ALREADY MSL (GPU kernel via mx.fast.metal_kernel); mx.compile removes Python from the per-step loop; MLX itself is C++. No Python tax on compute. C++ is a real lever specifically for: JIT-elimination (compiled primitive), hand-written backward control (loss backward 0008 + attention backward 0017), and upstreaming to mlx-core. Default stays Python+MSL; C++ primitive is a 0.2.0+ lever for the backward kernels.
  - **PROCEEDING TO 0.1.0 (Denis: "proceed to release movements"):** T17 step 4 calibration compute → bar-pinning STOP.
  - **BAR-PINNING STOP outcome (2026-07-08):** presented throughput bar (ours ~8-12% slower/step at gc=True: 0.923x seq1024 / 0.877x seq2048; recommended README "~8-12% slower", 0002 gate ours>=0.85x) + memory story (loss saves logit buffer at fixed context, ~0.72 GB seq2048; max context attention-bound, North-Star tied ~8500) + the PLANNER PROBLEM. **Denis chose: "Fix the model properly now"** (add O(N²) attention term + grad_checkpoint branch, refit from >=3 configs, validate vs North-Star). Delays 0.1.0 — accepted.
  - **PLANNER MEMORY MODEL — designed + MEASURED + CROSS-MODEL VALIDATED (2026-07-08):** model `marginal ≈ base + a_lin[gc]·batch·seq·hidden·layers + a_quad·batch·heads·seq² + analytic(lora+opt+loss)`; a_quad is ONE layer for both gc (sequential backward), gc changes only the linear term. Fit from Qwen3-8B North-Star gc=True (5 pts, RMSE 0.21 GB) + gc=False (2 pts). **Final constants:** base=1648034236 B (1.535 GB), a_lin_ckpt=1.867, a_lin_full=85.12 (45.6× ckpt), a_quad=8.716, optimizer=8.0 (analytic AdamW). **Cross-model validation:** ran Llama-3.2-3B-4bit gc=True (bg b6johhhpk, seq 1024/4096, marg 2.08/5.04 GB) — Qwen coeffs predict it within **±9%** (1.89/5.42 pred). Model is physical, generalizes. ±9% = the planner-accuracy tolerance to pin.
  - **PLANNER REWORK COMPLETE (2026-07-08, main thread — the plan-driven-coder subagent died on an API session limit before doing work, so controller implemented under TDD):** estimate.py now models `base + a_lin[gc]·batch·seq·hidden·layers + a_quad·batch·heads·seq² + analytic(lora/opt/loss)`, ×(1+overhead 0.10 safety); `_attention_bytes` added; `fit_activation_and_optimizer_bytes`→`fit_memory_coeffs` (3-var OLS gc=True → base/a_lin_ckpt/a_quad; 1-var gc=False → a_lin_full; optimizer analytic 8). calibration.py new fields; calibration_data.json measured constants (base 1600848316, a_lin_ckpt 1.867, a_lin_full 85.12, a_quad 8.716, opt 8.0). fit_calibration.py updated (grad_checkpoint manifest field). Tests: fit-recovery rewritten, +quadratic-scaling, +gc-linear-only, +measured-vs-predicted anchor (Qwen3-8B seq8192 within 15%, over-predicts=safe); test_cli budget bumped past the 1.6 GB base. **fit_memory_coeffs reproduces the hand-fit from real North-Star data; cross-model ±9% (Llama-3B).** Gates: 331 passed / 199 skipped, ruff + mypy --strict clean. **Committed 64c3017** (7 files). **T17 COMPLETE.** Branch dev/v0.1.0 = 13 commits ahead of origin (local-only, push fine). NEXT: **T18 docs + release PR** — README is a 6-line placeholder; no CHANGELOG/ROADMAP/LICENSE/publish.yml. Write with Denis's HONEST REFRAME framing (fused CE à la Cut-Cross-Entropy: never materializes (N,V) logits → loss-layer memory flat, frees headroom/batch at a given context, ~8-12% step cost, exact parity; MAX CONTEXT is attention-bound on MLX — see roadmap 0017 flash-attn-backward as flagship next). humanizer + leak-audit + review-docs/review-perf-claims/review-release, then release PR (STOP — Denis merges). (Was: IMPLEMENTATION DISPATCHED plan-driven-coder a49926, base HEAD 72699aa; brief `scratchpad/planner-model-rework-brief.md`.)
  - **(superseded dispatch note):** brief `scratchpad/planner-model-rework-brief.md`
- **T18 DOCS + RELEASE-GATE (2026-07-08, controller):** Denis APPROVED the README honest-reframe framing. Wrote README (honest: loss-layer ~3900x isolation + 1.64x fwd; "what it does/doesn't buy you" = frees memory at a given context + ~8-12% step cost + exact parity, but max context is attention-bound O(N²) → roadmap flash-attn-backward; positioned as CCE/Liger ported to Metal), CHANGELOG (0.1.0), public ROADMAP (flash-attn-backward = flagship Planned), LICENSE (MIT), .github/workflows/publish.yml (tag→PyPI Trusted Publishing, env `pypi`). **LEAK AUDIT:** public-docs process-word grep CLEAN; fixed SYSTEMIC shipped-src docstring local-path leaks (mlx-train-perf-spike/... in artifacts.py/source.py/estimate.py/calibration.py → reworded to self-contained provenance). **SDIST LEAK FOUND+FIXED:** hatchling swept gitignored .superpowers/ (SDD ledger+review diffs) + .codegraph/ into the sdist → added `[tool.hatch.build.targets.sdist] include` allowlist (src/tests/scripts/.github + user docs); rebuilt, sdist CLEAN, wheel clean (py.typed ✓). **COVERAGE GATE PASSES: 528 passed/2 skip, 99.40%** (>=85). mlx-lm pin already correct (>=0.31.3,<0.32, transformers<5.13). Committing: (1) src docstring cleanup, (2) release docs + sdist config. NEXT: review fan-out (review-docs/perf-claims/release + review-code/mlx/tests on the unreviewed planner-rework + bf16/gc changes) → CLAUDE.md refresh + backlog (0002/0009) → open release PR (STOP, Denis merges) → tag on merge (separate authorization). **PyPI pending-publisher for IonDen/mlx-train-perf (workflow publish.yml, env pypi) must be configured by Denis before the tag.** — rework estimate.py (activation gc-aware + new attention quad term + base) + calibration.py (new fields) + calibration_data.json (the constants above) + fit_activation_and_optimizer_bytes→fit_memory_coeffs + fit_calibration.py + all plan/fit tests, under TDD. NEXT: review the implementer's diff → then test_plan cross-model tolerance + commit → T18 docs (honest reframe). Extra bench dirs to note for cleanup: _artifacts/bench_train_step_derisk, _llama3b_gc, bench_train_step_gradckpt (keep — they're the calibration/validation evidence).
- **T18 REVIEW ROUND CLOSED (2026-07-08, resumed session):** the 3 watchdog-stalled reviewers re-dispatched with pre-built diff files and ALL completed. Verdicts + fixes (one commit, after RED→GREEN TDD on every code change):
  - **review-code (64c3017, opus): 1 Important + 1 Minor, rest verified clean** (formula/units/OLS hand-replayed against the measured anchors; fit↔estimate consistency confirmed). Important FIXED: `fit_memory_coeffs`'s count guard let a rank-deficient design through (3 pts / 2 distinct seq, batch fixed → lstsq minimum-norm garbage incl. NEGATIVE a_quad, reviewer-reproduced) → replaced with a `matrix_rank < 3` check on the actual design matrix; RED test (rejects 2-distinct-seq) + a batch-variation full-rank acceptance test (rank, not seq-count, is the condition). Minor FIXED: calibration.py/estimate.py module docstrings no longer call the measured coefficients "placeholders".
  - **review-tests (64c3017, sonnet): no dishonest tests; 2 Medium + 2 Low coverage gaps, ALL FIXED** (mutation-verified findings): exact-value pins for `components["base"]`/`activations`/`attention` (drivers are powers of two → float-exact ==), manifest `grad_checkpoint`-omitted→True default test, `optimizer_bytes_per_param` through `main()` assert. All four pins RED-proven via deliberate +1 perturbation, then corrected.
  - **review-mlx (fca1323+72699aa, sonnet): 1 High + 1 Low-latent, rest verified clean against installed mlx/mlx_lm source** (set_dtype floating-only predicate confirmed; bf16 quant scales fine incl. our MSL dq template; cast-before-LoRA ordering endorsed; fp16 refusal loud; gc threading + session-id sensitivity correct). High FIXED: `set_dtype` cast + LoRA init are LAZY and executed inside the `reset_peak_memory` window (contaminating marginal_peak_gb + step-1 wall) → `mx.eval(model.parameters())` after LoRA injection, before the window; RED ordering-spy test (call-through). Published bench numbers unaffected (setup transient ≪ multi-GB step peaks, identical both arms). Low: mlx_lm `grad_checkpoint()` patches class `__call__` un-reverted — latent, subprocess-isolated in prod; recorded as repo CLAUDE.md gotcha 13 (+14 for the lazy-cast lesson), no code change.
  - **review-perf-claims reword APPLIED:** README ceiling line now gives both arms (ours ~8450 / stock ~8700).
  - **Audit residue also fixed in the same commit:** northstar script comment + `--help` + test docstring no longer pre-bake "ours' max-context advantage" (finding 1 residue of the 2026-07-08 research-state audit); the mlx-pin question (audit finding 3: `mlx>=0.31.2` uncapped + verified-only 0.31.2 → loud kernel refusal on fresh installs resolving newer mlx) is surfaced as an explicit open question in the PR body — Denis decides cap-vs-verify-0.32 at merge/tag time.
  - **Gates re-run after fixes: ruff ✓, mypy --strict src ✓, default lane 337 passed, metal lane 534 passed / 2 skipped, coverage gate ✓** (src ~99%; repo TOTAL 92.6% because `--cov` also counts scripts/ — the earlier "99.40%" was the src-package figure, recorded here so nobody chases a phantom regression).
  - **Flash-attention review addendum processed (Denis-invoked /receiving-code-review):** finding verified (0017 is the right ticket; strategy docs hadn't absorbed it) → North Star blueprint → **rev 4** (pitch/North-Star/capability-table/roadmap/metrics rebased on the measured attention-bound ceiling; NEW C10 = flash-attn backward Metal kernel, 0.2.0 flagship, gated on 0017; moat risk added), backlog roadmap positioning paragraph corrected, prior audit + rereview finding-2 wording narrowed with dated correction notes. All executing Denis's existing 2026-07-08 rulings — no new decisions taken.
  - **Session bookkeeping same day:** repo CLAUDE.md refreshed (0.1.0 state, planner model, honest reframe, gotchas 13–14); backlog 0002 criteria ticked + closing note (disproven criterion annotated as superseded, not silently dropped), 0009 → done (real-checkpoint verification landed via T17 benches), 0017 parts 1+2 recorded; roadmap index updated (0009 → Done table, honest-reframe decision block).
- Task 12 PREP (verified 2026-07-04, ahead of dispatch — installed-contract facts, cite as site-packages/mlx_lm paths):
  - PIN: mlx-lm==0.31.3 (latest on PyPI, installed) but transformers must be >=5.0,<5.13 — 5.13.0 BREAKS `import mlx_lm` (tokenizer_utils.py:505 AutoTokenizer.register passes a string; transformers 5.13 auto_factory.py:680 requires a class). Bisected in an isolated venv: 5.0.0/5.4.0/5.8.0/5.11.0/5.12.0 all import OK, 5.13.0 fails. pyproject `mlx-lm` extra + dev group need the transformers upper bound; repo venv currently has 5.13.0 → fix venv (uv) BETWEEN implementer dispatches, keep mlx==0.31.2.
  - CONTRACT DRIFT vs plan text: `default_loss(model, batch, lengths) -> (ce, ntoks)` at tuner/trainer.py:86-99 — `lengths` is a TWO-COLUMN array (offsets, lengths) zipped at iterate_batches (trainer.py:170); mask = `(steps >= lengths[:,0:1]) & (steps <= lengths[:,1:])`, steps = arange(1, T+1) — offset-aware prompt masking, NOT the old `steps < lengths` form. ntoks = mask.sum(); ce summed fp32 / ntoks. Adapter must reproduce THIS mask exactly.
  - Injection point: `train(..., loss: callable = default_loss, ...)` consumed via `nn.value_and_grad(model, loss)` (trainer.py:218-240).
  - `train()` calls `mx.set_wired_limit(max_recommended_working_set_size)` at entry (trainer.py:229-230) — overrides our stricter caps; T14's IN-LOOP wired-cap re-assert is confirmed load-bearing, cite this line in T14's brief too.
- Task 10c: complete (commit 622d922, review ✅ Approved — ramp arithmetic hand-verified incl. exact-budget boundary consistency with check_budget; false-refusal regression traced stage-by-stage; both disclosed TDD-ordering slips adjudicated credible with non-tautological tests; 120 metal tests, launch.py 100% cov)
  - Minor for final review: calibrate's max_stages-exhaustion path (candidate>tile every iteration) untested — traced safe (over-provisions sustain reps only, returned rate unaffected); add a unit test for completeness.
  - Reviewer noted pre-existing "T8 review finding" comment in _probe_hidden (comment-scope, moved verbatim per brief; fine to leave).
  - 2026-07-04 rung 2 + 2b: **BAR MET — hoisted quant RT=4 = 2019.5 G at tile 8192 = 1.20x vs dense (<=1.5x threshold)**; parity OK both RTs (fp32 ablation exact 0.0); memory gate holds (0.389 GB resident). RECORD CORRECTION: plain quant RT=4 full-vocab = 1363.4 G — T10's 5.8x was probe-bias fiction, honest miss was 1.78x, the tile-8192 refusal was FALSE (probe 511 G vs full 1363 G). Disproven-prediction rule engaged: items 0011 (probe bias — production guard bug risk) + 0012 (ceiling-not-a-rate-predictor) filed with shared review artifact; user-metal-kernels updated same-day. NEXT: graduate hoist into source.py under TDD + re-run committed threshold bench + wire 0011 probe fix before T11.
  - Minor for final review: mypy typechecks at 3.12 while requires-python floor is 3.11 (numpy-stub constraint); runtime 3.11 compat covered by tests only
  - Plan's pyproject mypy block updated to match reality (mlx.* stubs visible; numpy scoped override)
